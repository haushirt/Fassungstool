# Backlog

Gesammelte Punkte aus allen Rollen. Erledigtes nicht löschen, sondern nach „Erledigt" verschieben – so bleibt nachvollziehbar, wer was wann behoben hat.

Spalten: Priorität · Rolle (wer hat es gemeldet) · Runde · Punkt · Datei:Zeile

## Hoch

| Rolle | Runde | Punkt | Datei:Zeile |
|---|---|---|---|
| qa-guardian | 16 | **MERGE-SPERRE · Ohne Netz lässt sich KEIN Vorgang mehr abschliessen — auch keiner, der den Server nirgends braucht.** `finishNetz()` setzt `fb.disabled=!verbunden()` auf dem einzigen Abschlussknopf ALLER Modi (`public/index.html:2549`; `data-netz` gesetzt bei `:5324`), und `abschlussSchritt()` steigt bei `tag`/`fuellen` zusätzlich VOR dem Festschreiben aus (`:5489`). Gemessen mit `tests/qa-runde16-schluss.cjs` (Q1/Q2, Playwright, 390 px, `setOffline(true)`): Tagesfassung vollständig, kein offener Punkt → Knopf gesperrt, `S.tag.finished` bleibt `false`, **0 fertige Einträge in `hh_ausgang_v1`**; Kellerzählung ebenso, obwohl sie weder Z-Bericht noch Abgleich kennt. Das steht gegen Projektanleitung §9 („Offline ist der Normalfall, nicht die Ausnahme. Im Weinkeller gibt es kein Netz.“), gegen CLAUDE.md „Architektur, die bleibt“, gegen Regel 6 (Offline-Queue nicht aufweichen), gegen die eigene Hilfe der App (`:2031`: „ohne Netz geht sie hinaus, sobald wieder Empfang da ist“) und gegen den Kommentar, den dieselbe Datei bei F4 trägt (`:5678`: „Der Vortagsabgleich … gehoert ins Backoffice, nicht in den Keller ohne Netz“). Die Sperre ist auch technisch unnötig: `holeZBericht()` (`:5422`) fängt jeden Fehlschlag ab und gibt `null` zurück — daraufhin kommt ohnehin `popupFertig()`. **Was zu tun ist:** (1) `data-netz` vom Abschlussknopf nehmen bzw. `finishNetz()` nur noch den Hinweistext setzen lassen, nie `disabled`; (2) in `abschlussSchritt()` den Block `if(!verbunden()){…return;}` ersatzlos streichen; (3) `popupFertig()` um einen Satz ergänzen, wenn ohne Netz abgeschlossen wurde („Der Abgleich kommt, sobald wieder Empfang da ist“); (4) die Prüfungen R16/4 in `tests/runde16.test.mjs` und „ohne Netz ist er ausgegraut“ in `tests/ui-runde16.cjs` umdrehen; (5) `tests/qa-runde16-schluss.cjs` muss danach grün sein. | `public/index.html:2542`, `:2549`, `:5324`, `:5489`, `tests/qa-runde16-schluss.cjs` |
| qa-guardian | 16 | **Der Nachschlag `/api/code` räumt die Sperre nicht auf — eine geglückte Freigabe lässt die Fehlversuche stehen.** `anmelden()` löscht bei einem Treffer alle Fehlzeilen der IP (`src/index.js:224`), `codeNachschlagen()` tut das nicht (`:272`). Wer sich an der Freigabe neunmal vertippt und beim zehnten Mal richtig liegt, lässt die IP an der Schwelle stehen; der nächste Tippfehler irgendwo im Haus sperrt dann die ANMELDUNG für 15 Minuten. Das Haus teilt sich eine IP. Entweder räumt der Treffer auch hier auf, oder es steht ausdrücklich als Absicht im Kommentar. | `src/index.js:224`, `:272` |
| qa-guardian | 15 | **MERGE-SPERRE · Die Sonderentnahme im Zweig „Getränke" hat keinen Weg zum Grund — jede Getränke-Entnahme endet in der Code-Freigabe.** `rGrund()` wird nur aus `rNach()` gerufen, also nur im Wein-Zweig; der Getränke-Zweig rendert `rGetrMenge` (public/index.html:3114, :3658). `offenList()` verlangt den Grund aber für den ganzen Modus `nach` (:5104). Folge, gemessen und bebildert: Wer am Tor „Getränke" wählt, sieht die fünf Knöpfe nie, hat im Abschluss dauerhaft „Grund fehlt", und der Sprung des offenen Punktes (`offenZiel` → Schritt 0, :5068) landet wieder in derselben Liste ohne Knöpfe. Der Abschluss geht dann nur über „Trotzdem abschließen?" mit persönlichem Code, und der Vorgang trägt für die Leitung „Ohne Bestätigung freigegeben … Grund fehlt". Cola für die Küche ist Alltag. Beleg: `review/screens/qa15/390-sonderentnahme-getraenke-*.png`, Prüfung: `tests/qa-schluss.cjs`. Zwei saubere Wege: `rGrund(m)` auch in `rGetrMenge` (bzw. vor dem Zweigschalter) rufen — oder den Grund nur für den Zweig verlangen, in dem er gesetzt werden kann. | `public/index.html:3114`, `:3658`, `:5068`, `:5104` |
| qa-guardian | 15 | **Der Soll-Bestand „Gasteiner 0,25 l" ist von 7 auf 8 geändert — aus einem Gestaltungsgrund.** Die Änderung steht in `src/stamm.json`, `public/index.html` und `public/leitung.html` gleich (`{"stapel":[["gastklein",8]]}`), der Commit nennt als Anlass „Lade 1 mit gleich hohen Flaschenspalten" (d3972f8). `GSOLL` kommt aus genau dieser Zahl (public/index.html:3955) und geht über `gFehlt()` in die Nachfüllmenge, die als `gent` ins append-only Journal wandert. Hält das Fach real 7 Flaschen, bucht jedes Nachfüllen dauerhaft eine Flasche zu viel aus dem Lager — und zurücknehmen lässt sich das Journal nicht. Vor dem Merge von Casimir bestätigen lassen: Passen 8 Flaschen Gasteiner 0,25 in die Spalte? Wenn nein: zurück auf 7 und die Spaltenhöhe anders lösen. | `src/stamm.json` (GETR/laden/1/cols/7), `public/index.html:3955`, `public/leitung.html:624` |
| qa-guardian | 15 | **`review/ERGEBNIS.md` beschreibt nur Runde 13 und nennt `sw.js` v32 — der Stand ist v35 und enthält Runde 14.** Die Datei ist laut CLAUDE.md die PR-Beschreibung. Es fehlen: der Grund der Sonderentnahme (samt `ereignis.notiz`), die Stammdatenänderung Gasteiner, Laden/Weinzeile/Startseite. Außerdem steht dort zum Betriebstag „vergleicht in den Tagen um den Merge herum einmal um einen Tag versetzt" — der Backlog-Punkt darüber sagt (vom Jäger berichtigt) DAUERHAFT. So darf der Satz nicht in den PR. | `review/ERGEBNIS.md:10`, `:29`, `:147` |
| Fehlerbehebung | 18.09. | **ENTSCHEIDUNG NÖTIG · Der Betriebstag ist umgestellt, der Abgleich im Backoffice nicht — und das ist DAUERHAFT, nicht einmalig.** (Erste Fassung dieses Punktes sagte „einmalig". Falsch, vom Jäger berichtigt.) Bis heute früh trug ein Vorgang des Modus `tag` den VORTAG als Schlüssel (`blank()` rief `yest()`); genau darauf baut `abgleich(tag)` auf: Es paart Z-Bericht(X) mit Vorgang(X) — den Bericht des Betriebstags X mit der Fassung, die seinen Verbrauch nachgefüllt hat. Seit F1 trägt die Fassung den Tag, an dem sie gemacht wird. Die Paarung stimmt damit ab sofort **jeden Tag** um einen Tag nicht mehr, nicht nur um den Merge herum. Zwei Wege, beide brauchen Casimirs Entscheidung, keiner gehört in diese Runde: **(a)** `abgleich()` paart Z-Bericht(X) mit Vorgang(X+1) — die Fassung am Abend danach füllt den Verbrauch von X nach; **(b)** die Fassung trägt zusätzlich den Tag, dessen Verbrauch sie deckt. Bis dahin: Die Zahlen unter „Verkauf ↔ Fassung" sind um einen Tag versetzt zu lesen. | `public/index.html:1720` (`blank`), `public/leitung.html:1336` (`abgleich`), `:1511` (`letzterTag`) |
| Fehlerbehebung | 18.09. | **Die Bilder an bereits archivierten Protokollen sind weg.** F4 löscht die IndexedDB `hh_fotos` beim ersten Start nach dem Merge, EINMAL je Gerät. Protokolle im Archiv, die ein Foto der Fassungsliste trugen, zeigen es danach nicht mehr. Das ist unwiederbringlich und war die ausdrückliche Vorgabe („der Foto-Zwischenspeicher in der IndexedDB … Dann löschen"). Wer eines dieser Bilder noch braucht, holt es sich VOR dem Merge vom Gerät. | `public/index.html:4575` (`aufraeumenFotospeicher`) |
| Fehlerbehebung | 18.09. | **Das Backoffice läuft bei 320 und 375 px aus dem Bild.** Die Navigationsschublade („Fassungstool ›") steht dort bis 336 px, bei 320 px Fensterbreite. Erst sichtbar, seit `tests/ui-mass.cjs` auch diese Breiten misst. Kein Fund dieser Runde (das Backoffice ist das MacBook-Werkzeug), aber einer, sobald jemand es am Handy aufmacht — und der Link „Backoffice" aus F5 steht seit heute in der App. | `public/leitung.html` (`.navknopf`, `@media(max-width:900px)`) |
| Fehlerbehebung | 18.09. | **Drei Fehler, die es seit Monaten gibt und die niemand gemessen hat — jetzt behoben.** Sichtbar geworden, seit `tests/ui-mass.cjs` jeden Schritt jedes Modus in beiden Zweigen misst statt nur den ersten. (a) **Kellerzählung bei 320 px:** Reihen, Einzeln und Summe brauchen zusammen 308 px, verfügbar sind 288 — die Summe ragte 18 px aus dem Fenster, die Seite rollte waagrecht (338 > 320) und zog die Sprungleiste mit hinaus, 178 Stellen. (b) **Sprungleiste:** Knöpfe 36 px breit statt 44. (c) **Zählfelder `−`/`+`:** 40 px breit statt 44, und der Knopf „+ N weitere anzeigen" im Abschluss 36 px hoch statt 44 — im Service, spät abends. (d) **Die Sprungleiste lag unter der Statuszeile:** Sie steht `position:fixed` in der Mitte des FENSTERS, der klebende Kopf steht darüber — bei 320 × 568 deckte die Netzzeile die oberen Pixel des ersten Knopfes zu. Wer im Keller „Rot" oben antippte, traf die Statuszeile; fünf von zwölf Tastpunkten. Jetzt wird die Leiste in der Mitte des Raumes UNTER dem Kopf zentriert, dessen Höhe zur Laufzeit gemessen wird. Alle vier behoben; die Lehre steht darüber: **Eine Messung, die nur die Eingangstür jedes Modus ansieht, sagt über das Haus nichts.** | `public/index.html:701` (`.zsteppers`), `:509` (`.rail`), `:713` (`.zbtn`), `:4508` (`btn--klein`), `:3316` (`buildRail`) |
| Fehlerbehebung | 18.09. | **Die Laden sind deutlich höher geworden — der Preis von F2.** Lade 4 bei 390 px: 608 → 1040 px, bei 320 px 608 → 1499 px; Lade 6 bei 390 px 638 → 1078 px. Ursache ist die Vorgabe selbst („Passt eine Spalte nicht, bricht das Raster um"): acht physische Spalten stehen bei 390 px als 4 über 4. Gewonnen ist, dass jeder Name unter seiner Flasche steht und nichts beschnitten wird; verloren ist, dass eine Lade auf einen Schirm passt. Braucht eine Antwort aus dem Service: lieber rollen oder lieber kleinere Punkte? | `public/index.html:3861` (`DHOCH` in `gFit`) |
| Fehlerbehebung | 18.09. | **Eine Spalte mit zwei Sorten trägt zwei Kopfzahlen ohne Beschriftung.** Lade 6, „Red Ale / Pale Ale": in der Kopfzeile steht „4/4 4/4". Links ist die obere Sorte, rechts die untere — das steht nirgends, es steht nur im `title`. Vorher war es positionell klarer (Zahl, Stapel, Zahl, Stapel), dafür standen die Kopfzahlen nicht auf einer Linie (F2). Braucht eine Antwort aus dem Service, nicht aus dem Code. | `public/index.html:3888` (`drwBox`, `.gnums`) |
| Jäger | 8 | **Ein abgelehnter Fremdgerät-Dialog öffnet einen Weg, den es vorher nicht gab.** `fernNeuer()` meldet einen fremden Stand nur, solange `fremd.zaehlnr > eigene`; die Zählnummer wächst je Gerät bei jedem Zwischenstand (alle 45 s). Gerät B lehnt ab, arbeitet sechs Zwischenstände, überholt damit Gerät A, der 409-Wächter greift nicht mehr und `ereignisseAbleiten` bucht die Lieferung von A gegen. A bekommt kein Signal, der Dialog sagt beim Ablehnen nichts darüber. | `public/index.html:2246`, `src/index.js:183` |
| Jäger | 8 | **Nach dem Sammelklick bleibt der Getränke-Abgleich vollständig stumm.** Der Knopf nimmt an Bericht 37 genau 5 von 18 Positionen; die 13, die stehenbleiben, sind exakt die 13 Getränke des Live-Mappings. Der Satz daneben nennt als Grund „dort fehlt die Ausschankmenge im Kassennamen" — bei allen 13 steht sie im Namen. Es fehlt die Gebindegröße, und ein Feld dafür gibt es nirgends. | `public/leitung.html` (Sammelknopf, Block „Größe fehlt") |
| software-engineer | 7 | **Der Fremdgerät-Dialog lässt sich nicht ablehnen.** Im Dialog „Auf einem anderen Gerät weiter?" setzt der Abbrechen-Zweig `start._uebernommen=false; start(m);`; `fernNeuer(m)` liefert danach denselben fremden Stand und der Dialog öffnet sich sofort wieder. Die Berichtigung ist ein Wort. In Runde 8 in Arbeit. | `public/index.html:2238` |
| software-engineer | 6 | **Getränke ohne Größe im Artikelnamen haben keinen Weg, ihre Gebindegröße zu bekommen.** Seit Runde 6 rechnet nur mit, was bestätigt ist — für Sanbitter, Cola, Almdudler und Gasteiner still gibt es aber keinen Vorschlag und damit keinen Knopf. Sie bleiben dauerhaft aus der Rechnung. Es fehlt ein Zahlenfeld „Gebindegröße ml" neben „Übernehmen"; der Endpunkt nimmt die Zahl bereits an, keine Migration nötig. | `public/leitung.html` (`vAbgleich`, Block „Größe fehlt"), `src/index.js:483` |
| software-engineer | 6 | **In den Stammdaten gibt es keine Spirituosen.** „Amaro Averna 2 cl", „Amaretto Sour", „Pisco Sour", „Gin Basil Smash", „Monkey Sour" — im echten Bericht 20 Stück und 244,50 € — können keinem Artikel zugeordnet werden und bleiben ewig „offen". Gehört zu §8 A und Entscheidung Nr. 6. | `public/leitung.html` (`STAMM`) |
| Moderation | 6 | **`kistenGr()` im Backoffice liest ein Feld, das es nie gab.** `p.kg` statt `p.kistengr` (die App legt `kistengr` ab); der Worker liest seit Runde 5 beide. Dieselbe Lieferung steht im Journal mit 40 und auf dem Schirm mit 12 Flaschen. In Runde 7 in Arbeit. | `public/leitung.html:678`, `public/index.html:2910`, `src/index.js` |
| controller | 6 | **Zwei Vorgänge desselben Modus am selben Tag löschen einander.** `start()` legt `blank(m)` mit demselben `tag` an, also denselben Schlüssel `<modus>_<tag>`; `ereignisseAbleiten` nimmt den ersten per Gegenbuchung aus dem Bestand. Zwei Lieferungen an einem Tag sind normal; live existiert bereits `ware_2026-09-16`. In Runde 7 in Arbeit. | `public/index.html:2230`, `src/index.js:262` |
| controller | 6 | **`bestand()` schlüsselt nicht auf `ort`.** Sobald ein zweiter Zählort entsteht (Entscheidung Nr. 14, Getränkelager), wird die jüngere Zählung zum Anker für beide Orte. Der Index `(artikel, ort, ts)` ist live schon breit genug — das Schema kann es, der Code nicht. | `src/index.js:373`, `public/leitung.html` (`bestand`) |
| software-engineer | 5 | **Der Z-Import ist nicht atomar.** Kopf, `DELETE` und Zeilen sind drei Schreibvorgänge. Mit eingespeistem Fehler im Zeilen-`batch` bleibt der Kopf mit frischem `importiert` und NULL Positionen stehen — die Leitung sieht einen eingelesenen Tag ohne Inhalt. Gehört wie `vorgangSchreiben` in EIN `batch`. | `src/index.js:397–422` |
| software-engineer | 5 | **Ein Teilbericht desselben Tages ersetzt den vollen lautlos.** Zweimal derselbe Betriebstag ist richtig (ein Bericht je Tag), aber ein Bericht mit 2 Positionen überschreibt einen mit 48 ohne Rückfrage und mit HTTP 200. Mindestens melden, wenn der neue deutlich weniger Positionen hat. | `src/index.js:394` |
| Orchestrierung | 1 | `npm run deploy` (`wrangler deploy`) steht weiter in `package.json`. Regel 2 verbietet den Befehl, und laut Projektanleitung §6 kann genau er die nur im Dashboard gepflegten Einstellungen (D1-Bindung, ASSETS, ABSENDER, Crons) entfernen. Dieselbe Gefahrenklasse wie das entfernte `npm run schema`, vom qa-guardian übersehen. Deploy läuft ohnehin automatisch über Workers Builds. | `package.json:7` |
| Setup | – | Der Verwaltungs-Editor ist über den Menüknopf „Verwaltung" weiter erreichbar, obwohl Projektanleitung §8 A ihn als aus `index.html` entfernt beschreibt. Doku und Code widersprechen sich. Der Klartext-Code ist seit Runde 1 weg, der Editor selbst nicht. | `public/index.html:1836` (`bAdmin`), `:1899` (`askPin`) |
| software-engineer | 1 | Verwaltungs-Editor nach `leitung.html` umziehen (§9.2 A) und `hh_cfg_v9` von localStorage nach D1 `stamm` heben, damit Glasweine und Soll-Mengen für alle Geräte gelten. **Seit Runde 3 nicht mehr blockiert:** `stamm` hat live `schluessel, wert, geaendert, wer` – `GET /api/stamm` liest sie richtig, ein Schreibweg fehlt noch ganz (kein `POST /api/stamm` im Worker). | `public/index.html:1933` (`renderAdmin`) → `public/leitung.html` |
| hospitality-pro | 1 | Soll-Mengen und Glasweine kann seit Runde 1 jede Servicekraft ändern (`bekannterCode()`), die Änderung ist ungezeichnet (`oninput`→`saveCfg`, kein Name, keine Zeit, kein Journal) und gilt nur auf einem Gerät. Damit rechnen iPad, iPhone und MacBook mit verschiedenen Soll-Werten – und die Fehlmenge, die jeden Tag aus dem Keller geholt wird, hängt davon ab, wer fasst. Entscheidung: `review/OFFENE-ENTSCHEIDUNGEN.md` Nr. 6. | `public/index.html:1899` (`askPin`), `:1933` (`renderAdmin`), `:1956` (`.aSoll` `oninput`) |
| hospitality-pro | 1 | Die Sonderentnahme erfasst keinen Grund. Bruch, Personalgetränk, Küche, Verkostung und Zimmer werden betrieblich völlig verschieden behandelt; ohne Grund landet alles im Wareneinsatz Getränke. Der Grund steht heute im optionalen Freitextfeld ganz am Ende – das füllt nach dem Service niemand mehr aus. Entscheidung: `review/OFFENE-ENTSCHEIDUNGEN.md` Nr. 7. | `public/index.html:1364` (Kachel `nach`), `:3746` (Notiz) |
| Setup | – | `ANLAGE_OFFEN` im Dashboard steht seit 01.09. offen – Selbstanlage eines Kontos mit beliebiger Rolle. Nur von dir lösbar (Regel 13). | Dashboard |
| Setup | – | Keine Sicherung der D1. Ein falsches `DELETE` in der Console löscht den Kellerbestand unwiederbringlich (Projektanleitung §8). | Dashboard |
| Setup | – | `tests/fixtures/` fehlt. QA und Controller können ohne die anonymisierten Z-Berichte und Zählungen nicht rechnerisch prüfen. | – |
| Hauptsitzung | 4 | **Die Umstellung auf längere Codes wirkt erst, wenn jedes Gerät sich einmal neu anmeldet.** Der Vorrat `hh_bekannt_v1` räumt den ersetzten Code weg, sobald der neue beim Server bestätigt wurde (`merkeCode`) — ein iPad, das danach nie wieder angemeldet wird, kennt den alten Code ohne Netz weiter und öffnet damit Verwaltung und Freigabe. Beim Vergeben der neuen Codes jedes Gerät einmal anfassen. Nur von dir lösbar (Regel 13). | `public/index.html:1369` (`merkeCode`), Geräte |
| qa-guardian | 1 | Die vier persönlichen Codes stehen weiterhin im Klartext in der Git-Historie (`public/index.html`, zwei ältere Stände, `const USER={…}`) und wurden bis v11 öffentlich ausgeliefert. Aus HEAD sind sie weg, aus der Geschichte nicht – und umschreiben darf sie niemand (Regel 1: kein force-push). Die vier Codes sind damit als bekannt zu behandeln: neue Codes vergeben, und nicht wieder vierstellig. Nur von dir lösbar (Regel 13). | Dashboard / D1 `person` |
| hospitality-pro | 2 | Der Fotoschritt der Fassungsliste treibt die **Freigabe**: Ist alles geprüft, alles geholt und nur das Foto fehlt, sagt der Abschluss „Ein Punkt ist noch offen" und „Fertig – Protokoll erstellen" führt in „Trotzdem abschließen?" mit Code. Im Protokoll steht danach „Ohne Bestätigung freigegeben von …" auf einer tadellosen Tagesfassung. Die Servicekraft lernt, dass die Freigabe der normale Weg ist; die Leitung lernt, die Warnung zu überlesen. Im Browser durchgespielt. Entscheidung: `review/OFFENE-ENTSCHEIDUNGEN.md` Nr. 9. | `public/index.html:4044` (`offenList`, Punkt Fassungsliste), `:3911` (`fb.onclick`) |
| software-engineer | 2 | `/api/anmelden` liefert die `rolle` mit, die App wirft sie weg: `merkeCode(code,name)` legt nur den Namen ab. Ohne gemerkte Rolle ist offline im Keller keine Rechteprüfung möglich – das ist die technische Voraussetzung für Entscheidung Nr. 6 und in wenigen Zeilen zu haben (Wert als `{name, rolle}` ablegen, alte Zeichenketten weiter lesen). Bewusst nicht in Runde 2 gebaut, weil Nr. 6 offen ist und der Editor nicht vorab eingeschränkt werden soll. | `public/index.html:1325` (`merkeCode`), `:1797` (Anmeldung wirft `j.rolle` weg) |

| qa-guardian | 2 | **Die Offline-Reihe hat keinen Fehlversuchszähler.** `inDenAusgang` legt `versuche:0` an (`public/index.html:1560`); das Feld wird an keiner Stelle im Repo gelesen oder erhöht (`grep versuche` findet genau diese eine Zeile). Ein Paket, das der Server dauerhaft mit 500 ablehnt, bleibt damit für immer vorn in der Reihe, hält alles dahinter auf, wird alle 45 Sekunden neu versucht — ohne wachsenden Abstand, ohne Zahl, die jemand ansehen könnte, und mit der immer gleichen Zeile „Serverfehler". Nichts geht verloren (das ist richtig, Regel 6), aber niemand erfährt, dass etwas feststeckt. Durch die Gegenbuchung ist der 500 aus einem gedachten zu einem greifbaren Fall geworden (siehe Punkt darüber). Prüfung hält den Zustand fest: `tests/ausgang.test.mjs` „dauerhafter 500 … (bekannt)". | `public/index.html:1560` (`inDenAusgang`), `:1594` (`schiebe`) |

| software-engineer | 3 | **Jede Fassung, die seit dem Livegang gezählt wurde, steht nur auf den Geräten.** In der D1 sind `vorgang`, `ereignis`, `fassungsliste`, `stamm` und `mapping` leer – es ist nie eine angekommen. Nach dem Livegang dieses Standes wird die Offline-Reihe der Geräte (`hh_ausgang_v1`) ihre liegengebliebenen Pakete von selbst nachschicken: `schiebe()` versucht alle 45 s weiter und verwirft nichts (Regel 6). Zu erwarten ist also ein Schwall alter Vorgänge, jeder mit seinem eigenen `<modus>_<tag>`. Das ist richtig so – aber es sollte jemand zusehen, und die Geräte sollten vorher **nicht** aufgeräumt werden. | `public/index.html:1594` (`schiebe`) |
| Hauptsitzung | 4 | **Der Rückfall der Blockwahl bleibt blind für gespaltene Berichte.** Seit dem echten Bericht Nr. 37 wird der Positionsblock beim Namen gewählt („Positionen"/„Artikel"/„Artikelumsätze"). Fehlt dieser Name — anderes gastronovi-Layout, andere Kasse —, gilt weiter „grösster Block gewinnt", und ein gespaltener Bericht verliert die kleinere Hälfte ohne Meldung. Für Haus Hirt ist der Fall nicht eingetreten (Nr. 37 hat einen Block); die Lücke bleibt, bis jemand entscheidet, ob mehrere Blöcke summiert werden sollen (Entscheidung Nr. 12). | `src/gnparse.js:139` (Blockwahl) |
| qa-guardian | 3 | ~~**Ein nach Kostenstellen gespaltener Z-Bericht wird nur zur Hälfte gelesen.**~~ **Am echten Bericht geprüft: Nr. 37 ist nicht gespalten** (ein Block „Positionen", Bar und Restaurant nur als Tagessumme). Der Punkt bleibt als Rückfall-Lücke bestehen, siehe die Zeile darüber. Ursprünglicher Fund: `parseZ` wählt genau EINE Sektion — die mit den meisten Positionszeilen — und verwirft alle anderen ohne Meldung. Nachgestellt: „Restaurant" + „Bar" als zwei Blöcke → 8 Achtel und der ganze Gin Tonic fehlen, 179,50 € statt 345,50 €. Der Import meldet Erfolg. Damit vergleicht „Verkauf ↔ Fassung" jede Nacht den halben Ausschank mit der ganzen Fassung, und die Differenz sieht aus wie Schwund. Ob der echte Bericht so aussieht, weiss niemand — `tests/fixtures/` ist im vierten Zug leer. **Nicht auf Verdacht geändert** (Regel 7). Verhalten festgehalten in `tests/zbericht.test.mjs` („welcher Block gewinnt"), Entscheidung in `review/OFFENE-ENTSCHEIDUNGEN.md` Nr. 12. | `src/gnparse.js:114` (Blockwahl) |


## Mittel

| Rolle | Runde | Punkt | Datei:Zeile |
|---|---|---|---|
| qa-guardian | 16 | **`kurz()` in `leitung.html` sichert nur den KOPF, nicht den Leib — „PIN zurücksetzen" hängt dann unbegrenzt.** Der `AbortController` wird im `finally` geräumt, sobald `fetch` auflöst, und das ist, sobald die Kopfzeilen da sind; `leitung.html:2741` ruft danach `await r.json()` ohne jede Frist. Gemessen (`tests/qa-runde16-stummer-leib.cjs`, Server sendet `200` + `content-type` und den Leib nie): `kurz()` kommt nach 3 ms zurück, der Aufrufer hängt danach unbegrenzt — `b.disabled=false` am Ende des Handlers wird nie erreicht, der Knopf bleibt tot, es erscheint weder Dialog noch Hinweis, und der `catch`-Zweig mit dem Ersatzcode (`vorschlag`) greift nicht, weil nichts wirft. Der neue Code kann am Server längst stehen: Die Person kann sich dann nicht anmelden und niemand kennt ihren Code. Der Kommentar bei `:927` behauptet „Dieselbe Rechnung wie `holeKurz()` in index.html" — dort wird der Leib seit `79d8bc7` INNERHALB der Frist gelesen, hier nicht. Gegenprobe im selben Lauf: `holeKurz()` kommt bei zurückgehaltenem Leib nach 8001 ms zurück. | `public/leitung.html:927`, `:2741` |
| qa-guardian | 16 | **Eine ohne Netz vorgemerkte Abmeldung wird nur beim Neuladen nachgeholt, nicht im laufenden Fenster.** Beim Start steht `if(!whoAmI())holeAbmeldungNach();` (`public/index.html:5944`) — das deckt den Normalfall ab und ist richtig so. Der `online`-Horcher, der sie sonst nachholt, wird aber in `netzStart()` gesetzt (`:2588`), und `netzStart()` läuft nur, wenn jemand angemeldet ist (`:5939`). Bleibt das Fenster nach einer Abmeldung ohne Netz offen und kommt der Empfang zurück, wartet `hh_abmeldung_offen_v1` bis zum nächsten Neuladen oder bis sich jemand anmeldet. Bis dahin lebt der Sitzungskeks (zwölf Stunden) und `/leitung.html` ist direkt erreichbar. Ein `window.addEventListener("online", holeAbmeldungNach)` neben `netState` (`:5932`) schliesst es. | `public/index.html:2588`, `:5932`, `:5944` |
| qa-guardian | 16 | **`anderePersonen()` übersieht eine Zeile mit `id IS NULL`.** `WHERE id != ?1` ist für NULL weder wahr noch falsch; SQLite lässt in `person` NULL als PRIMARY KEY zu (`docs/live-schema.sql:34`, TEXT PRIMARY KEY ohne NOT NULL). Eine solche Zeile würde bei der Dopplungsprüfung von `personSchreiben()` und `pinZuruecksetzen()` übergangen — zwei Personen mit demselben Code, und im append-only Journal steht dauerhaft der falsche Name. `WHERE id IS NULL OR id != ?1` schliesst das. Heute legt nur der Worker Personen an (immer mit UUID), der Fall ist also theoretisch. | `src/index.js:715` |
| qa-guardian | 15 | **`sw.js`: `activate` löscht den alten Vorrat, auch wenn `install` nichts einsammeln konnte.** Jeder Eintrag des Gerüsts wird einzeln mit `.catch(()=>{})` geholt (public/sw.js:44). Bricht die Verbindung zwischen dem Laden von `sw.js` und dem Füllen ab, gilt die Installation als geglückt, der neue Speicher ist leer — und `activate` räumt den alten weg (:53). Das Gerät steht dann im Keller ohne Netz und ohne Kopie. Vorschlag: den alten Vorrat nur löschen, wenn `/index.html` im neuen wirklich liegt. Vorbestehend, aber v31 → v35 lässt jedes Gerät genau durch diese Stelle laufen. | `public/sw.js:44`, `:53` |
| qa-guardian | 15 | **Der Grund landet im Journal, aber nirgends im Backoffice.** `grundNotiz` schreibt `grund=<key>` nach `ereignis.notiz` (src/index.js:262). `public/leitung.html` liest `ereignis.notiz` an keiner Stelle — die Leitung sieht den Grund nur im Protokoll des einzelnen Vorgangs (`resultText`, public/index.html:5247). Die Begründung der Runde („damit das Backoffice sie gruppieren kann") ist damit noch nicht eingelöst. | `src/index.js:262`, `public/leitung.html` |
| qa-guardian | 15 | **Eine Sonderentnahme nur mit Getränken zeigt im Abschluss eine große „0".** `summaryBig()` rechnet für `nach` allein `sum(d.ent)`, also nur Wein (public/index.html:4920). Vorbestehend; fällt aber jetzt zusammen mit „Grund fehlt" ins Auge — beides auf demselben Schirm. | `public/index.html:4920` |
| software-engineer | 11 | **„Nachbestellen" und der Mittagsblick nennen die am Zähltag gelieferte, nicht gerechnete Menge nicht.** Seit v30 ist der Bestand am Zähltag bewusst zu niedrig (Entscheidung Nr. 10); die Bestellliste rechnet darauf und kann eine Bestellung vorschlagen, die gerade geliefert wurde. Entweder die Angabe auch dort, oder die Bestellliste rechnet `unklar` als Spanne aus. | `public/leitung.html` (`bestellliste`, `vHeute`) |
| software-engineer | 11 | **Erfasster Zählzeitpunkt statt Ankunftszeit.** `vorgang.begonnen` trägt ihn NICHT (`src/index.js:205` bindet `jetzt`, die App legt in `blank()` gar keinen Startzeitpunkt ab). Zweiteilig zu bauen: die App schreibt beim Anlegen einen Gerätezeitpunkt in den Vorgang, der Worker führt ihn nach `begonnen` und ins Journal. Erst damit ist „Zählung oder Lieferung zuerst?" entscheidbar statt ausweisbar. | `public/index.html` (`blank`), `src/index.js:205` |
| software-engineer | 10 | **`bestand()` gibt es weiter zweimal** (Worker und Backoffice). Beide rechnen jetzt nachweislich dieselbe Zahl (`tests/reihenfolge.test.mjs` prüft sie gegeneinander), aber `/api/bestand` hat außer den Prüfungen keinen Leser — die Leitung sollte ihn lesen, statt die Regel ein zweites Mal zu halten. | `src/index.js` (`bestand`), `public/leitung.html` (`bestand`) |
| ui-designer | 9 | **Die Datumzeile im App-Kopf bricht bei langen Wochentagen um** (248 px Text, 240 px Spalte) — seit der Schriftanhebung auf 15 px. Entweder Wochentag kürzen oder der Zeile eine eigene Reihe geben; nicht die Schrift zurückdrehen. | `public/index.html:916` |
| ui-designer | 9 | **Das 46-px-Raster der Zählpunkte kostet rund 24 px Namensspalte**; „Rubin Carnuntum" bricht bei 390 px auf zwei Zeilen. Abhilfe nur über die Platzierung der Fehlmenge (über den Punkten statt daneben), nicht über kleinere Griffe. | `public/index.html` (`.dots`, `.miss-n`) |
| software-engineer | 7 | **„Trotzdem neu beginnen" ist angesagt, aber nicht abgesichert:** es nimmt die erste Lieferung im Journal per Gegenbuchung zurück. Prüfen, ob es stattdessen eine Korrekturbedienung im fortgeführten Vorgang braucht. | `public/index.html` (`start()`) |
| software-engineer | 7 | **Rezepturen für „1 Glas"-Positionen fehlen.** Ohne sie bleibt der Verkauf von Aperol Spritz, Pisco Sour und den übrigen Mischgetränken dauerhaft außerhalb der Rechnung — im echten Bericht 20 Stück und 244,50 €. `REZ` liegt nur im Browser der Leitung, die Spalte `mapping.rezept` gibt es nicht (Migration 001 steht aus). | `public/leitung.html` (`vRezepte`), `migrations/001_mapping_rezept.sql` |
| Jäger | 6 | **Widersprechen sich zwei Bestätigungen desselben Artikels, fällt `gebindeGroesse` still auf den 750-Vorschlag zurück** statt auf „unbekannt": `gebArtikelBestaetigt()` setzt `t[id]=null`, `if(ueberArtikel)` ist damit falsch und der Weg läuft bis Rang 4 durch. Folge heute nur: der Knopf bietet 750 ml an, wo nichts bekannt ist. | `public/leitung.html:647`, `:663` |
| Jäger | 6 | **Eine Zählung auf 0 erzeugt keine Journalzeile** (`if (menge)`): der leergezählte Wein behält im Journal seinen alten Stand. Heute ohne sichtbare Folge, weil `/api/bestand` keinen Verbraucher hat — mit Phase B sofort relevant. | `src/index.js:269` |
| software-engineer | 6 | **`offen` zählt je Kassenname nur die erste Zeile.** Derselbe Name kommt im Bericht mehrfach vor (Regel 7, Bar und Restaurant getrennt gebucht: „Aperol Spritz 1 Glas" 4 + 2). `ohneGroesse` summiert richtig, `offen` nicht — die Zahl in der Navigation und in der CSV ist zu niedrig. | `public/leitung.html:1118` |
| software-engineer | 6 | **`mapping.gebinde_ml` hängt am Kassennamen, nicht am Artikel.** Die Regel „bestätigt für diesen Artikel" (`gebArtikelBestaetigt`) ist eine Krücke der Oberfläche. Ob die Gebindegröße nicht in die Stammdaten des Artikels gehört, ist eine Frage für Phase B — additiv lösbar. | `docs/live-schema.sql:102` |
| software-engineer | 6 | **Es gibt keine Warenart in den Stammdaten** (`GETR` kennt nur die Ladengeometrie; in Lade 6 liegen 0,33 l und 0,5 l nebeneinander). Solange das so ist, kann kein Standard je Warenart greifen — deshalb steht in `GEBINDE_STANDARD` nur der Wein. | `public/leitung.html` (`STAMM.GETR`) |
| controller | 6 | **Die Marke der Gegenbuchung muss `grund` enthalten, sobald es ihn gibt** — sonst falten zwei Entnahmen desselben Artikels mit verschiedenem Grund zusammen und die Korrekturrechnung bucht eine Differenz, die es nicht gibt. | `src/index.js:288`, `:311` |
| controller | 6 | **Sonderentnahme „Zimmer"/„Verkostung" kann doppelt zählen**, wenn dieselbe Flasche über die Kasse läuft (Z-37: Rabatt „Welcomedrink", 0-€-Zeilen). Ein Satz in der App: „Was über die Kasse läuft, wird hier nicht gemeldet." | `public/index.html` (Kachel `nach`) |
| hospitality-pro | 5 | **Der Wareneingang von Getränken ist in keiner Ansicht des Backoffice zu sehen** — 48 gelieferte Flaschen Cola stehen im Journal und tauchen nirgends auf. Es gibt keinen Getränke-Lagerbestand und kein Nachbestellen für Getränke; die Ansicht „Getränke" zeigt nur die Laden der Bar. Hängt an Entscheidung Nr. 14. | `public/leitung.html` (`vGetraenke`), (`bestellliste`) |
| software-engineer | 5 | **Im Wareneingang verwirft ein Feldwechsel die gerade getippte Zahl.** Jedes `onblur` ruft `render()` und baut die Karte neu; wer den Jahrgang tippt und dann auf „Kisten" tippt, schreibt in ein Feld, das im selben Augenblick ersetzt wird. Gemessen mit echten Klicks: `{"jg":"2023","kisten":1}` statt 2. | `public/index.html:2907–2912` |
| hospitality-pro | 5 | **„Speicher": Wareneingang und Kellerzählung stehen mit „—" da**, obwohl 24 bzw. 57 Positionen dahinterstehen (`flWein`/`flGetr` zählen `eingang` und `zaehlung` nicht). Die Notiz einer Sonderentnahme — oft der ganze Inhalt — fehlt in der Liste ganz. | `public/leitung.html` (`vSpeicher`) |
| hospitality-pro | 5 | **Die Zuordnungsliste hat keinen Filter „nur offene" und kein Sammel-Ignorieren.** Die 12 Speisenamen (58 Stück) müssen einzeln weggeklickt werden, und Rabatt- und Stornogründe sind keine Kassenpositionen, stehen aber in der Liste. | `public/leitung.html` (`vZuordnung`) |
| hospitality-pro | 5 | **Die Reihenfolge der Ansichten ist nicht die des Arbeitstags** (Z-Bericht an dritter Stelle, „Zuordnung" mit roter Zahl unter „Nachschlagen"); der Tag beginnt mit einlesen → zuordnen → abgleichen. Dazu: „Mittagsblick" heisst der Schirm, den die Leitung morgens aufmacht; tote Funktion `vVorgaenge`; der Zeitraum des Abgleichs wird nicht gemerkt. | `public/leitung.html` |
| hospitality-pro | 5 | **§8 A bleibt offen:** Glasweine, Soll-Mengen, Kistengrößen, Kellerbereiche und neue Weine/Getränke sind nirgends änderbar — `STAMM`/`PLAN` stehen fest im HTML beider Seiten. Hängt an Entscheidung Nr. 6. | `public/leitung.html`, `public/index.html` |
| Hauptsitzung | 4 | Regel 7 in `CLAUDE.md` nennt vier gastronovi-Eigenheiten. Der echte Bericht Nr. 37 hat vier weitere gezeigt, die genauso wenig wegrefactort werden dürfen: Spaltenüberschrift als Sektionskopf, Rauten als Trenner, Z-Nummer in zwei Feldern, „1/8 l" als Achtel. Sie stehen jetzt in `tests/zbericht-37.test.mjs` und in den Kommentaren von `gnparse.js` — in den Regeln stehen sie nicht. Nur du darfst `CLAUDE.md` ändern (Vorschlag, keine Tat). | `CLAUDE.md` Regel 7 |
| hospitality-pro | 1 | Das Datum eines Vorgangs lässt sich im Kopf auf jeden beliebigen Tag stellen, auch in die Zukunft, ohne Rückfrage. Eine Tagesfassung auf dem falschen Tag erzeugt einen zweiten Vorgangsschlüssel und wird gegen den falschen Z-Bericht verglichen; in der Leitung sieht das am Morgen aus wie zwei Fassungen. Mindestens: Rückfrage, wenn das Datum mehr als einen Tag vom Vorschlag abweicht, und kein Datum in der Zukunft. | `public/index.html:4308` (`#dSel.onchange`) |
| hospitality-pro | 2 | Im Abschluss der Tagesfassung ist „Foto aufnehmen" der dunkle Hauptknopf und „Fertig – Protokoll erstellen" der helle daneben. Der auffälligste Knopf im letzten Schritt ist damit der Nebenschritt. **In Runde 3 nachgemessen (Browser, iPhone 390 px):** `.fotobtn.leer` steht auf `rgb(0,73,71)` mit weißer Schrift bei y=296, `.finishbtn.blocked` auf Weiß mit dunkler Schrift bei y=487 — beide 52 px hoch. Die Ursache ist also **nicht** eine verrutschte Rangfolge, sondern `.finishbtn.blocked`: Solange ein Punkt offen ist, hält sich der Abschlussknopf bewusst zurück. Sobald ein Foto da ist, drehen sich die beiden um (ebenfalls gemessen). Damit ist es genau die Kopplung aus Entscheidung Nr. 9: Fällt der Fotoschritt, löst sich der Punkt von selbst. Nicht einzeln anfassen. | `public/index.html:911` (`.fotobtn.leer`), `:286` (`.finishbtn.blocked`), `:4123` (`rFotos`) |
| qa-guardian | 1 | Die Frage „Auf einem anderen Gerät weiter?" holt mit „Übernehmen" auch einen bereits abgeschlossenen fremden Vorgang. `start()` archiviert ihn dann sofort (`cur.finished`), legt eine leere Fassung an und schickt den fremden Stand unter der eigenen Gerätekennung mit höherer Zählnummer noch einmal hinaus. Wer danach neu zählt, schreibt einen zweiten Stand über denselben Schlüssel – und das Journal nimmt ihn nicht mehr an (Punkt unter Hoch). `fernNeuer` sollte abgeschlossene Vorgänge anders behandeln als laufende. | `public/index.html:1590` (`fernNeuer`), `:2013` (`start`) |
| qa-guardian | 1 | `bekannterCode()` prüft nur, ob irgendein Code auf diesem Gerät schon einmal angemeldet war – nicht, ob es der Code der gerade angemeldeten Person ist. Wer am geteilten iPad den Code einer Kollegin kennt, kommt in Verwaltung und Freigabe, obwohl er selbst angemeldet ist; im Protokoll steht dann der Name aus `whoAmI()`. Mit Entscheidung Nr. 6 gemeinsam anfassen. | `public/index.html:1330` (`bekannterCode`) |
| software-engineer | 2 | Das Sackfach `hh_ueberholt_v1` hält den überholten Stand fest, aber niemand kann ihn ANSEHEN. Die Statuszeile sagt, dass er nicht übernommen wurde, und lässt ihn quittieren – was genau gezählt war, kommt nur noch über die Entwicklerkonsole ans Licht. Eine schlichte Ansicht („Was war in meinem Stand?") gehört dazu, sonst ist die Auskunft halb. | `public/index.html:1531` (`insSackfach`), `:1638` (`ueberholtSatz`) |
| software-engineer | 2 | Seit der Gegenbuchung kann `ereignis.menge` negativ sein (Korrektur nach unten). **Bestand geprüft (qa-guardian, Runde 2):** `bestand()` rechnet vorzeichenbehaftet, `public/leitung.html` liest `menge` nirgends (es gibt genau drei `FROM ereignis` im Repo, alle in `src/index.js`), und der Wochenbrief — bis dahin von keiner Prüfung je durchlaufen — rechnet ebenfalls richtig; belegt in `tests/wochenbrief.test.mjs`, 3 der 8 Prüfungen fallen, wenn man das Vorzeichen fallen lässt. **Offen bleibt der vorausschauende Teil:** jede KÜNFTIGE Auswertung, die `menge` als Betrag liest (Balken, Ranglisten, Export, alles in Phase B/C), muss das Vorzeichen mitdenken. | `src/index.js:296–308` (`korr`), `:330` (`bestand`), `:567` (Wochenbrief), `tests/wochenbrief.test.mjs` |
| Setup | – | `review/INPUT-TEAM.md` ist noch leer. Team-Anliegen haben laut `CLAUDE.md` Vorrang vor eigenen Ideen. | `review/INPUT-TEAM.md` |
| software-engineer | 1 | Die Offline-Queue bleibt bei einem dauerhaften 4xx stehen: Ein Paket, das der Server immer wieder mit 400/422 ablehnt, hält alle nachfolgenden Vorgänge auf und meldet nur „Server antwortet nicht". Stilles Verwerfen verbietet Regel 6 – also Sackfach plus sichtbarer Hinweis. Entscheidung nötig. | `public/index.html:1538` (`schiebe`) |
| software-engineer | 1 | `vorgangSchreiben` prüft nicht, ob der Pfad-Schlüssel zu `daten.mode`/`daten.tag` passt. Bei einer Abweichung stehen Spalte `tag` und Kennung auseinander – die Datumsfilter der Leitung und der Wochenbrief rechnen dann am falschen Tag. Erst nach dem Punkt darüber umsetzbar (422 würde die Queue blockieren). | `src/index.js:162` (`vorgangSchreiben`) |
| software-engineer | 1 | Kein Testgerüst im Repo (§10: Prüfserver, Mehrgeräte- und Gestaltungslauf). `node_modules` fehlt, `src/index.js` lässt sich lokal nur mit einer `postal-mime`-Attrappe importieren. | – |
| ui-designer | 1 | `askPin`, `#pinIn`, `.input--pin`: Der Verwaltungsdialog heißt im Code weiter „PIN", in der Oberfläche „persönlicher Code". Für den Nutzer unsichtbar, für den nächsten Leser irreführend — umbenennen, wenn der Editor ohnehin nach `leitung.html` zieht. | `public/index.html:1899` (`askPin`), `:1902` (`#pinIn`) |
| ui-designer | 1 | Der Menüknopf heißt „Verwaltung", der Dialog dahinter regelt Glasweine und Soll-Mengen. Ein Wort, das nichts verspricht. Beim Umzug ins Backoffice besser „Glasweine und Soll-Mengen". | `public/index.html:1836` (`bAdmin`) |
| ui-designer | 1 | Dialoge mit Code-Feld (`.card2`, `max-height:88vh`) können mit offener Zifferntastatur länger sein als der sichtbare Bereich — der Knopf „Freigeben" landet dann hinter der Tastatur. Betrifft vor allem die Freigabe mit vielen offenen Punkten. Braucht einen Browsertest auf echtem Gerät. | `public/index.html:748` (`.card2`) |
| ui-designer | 1 | `.ov` ist kein Dialog im Sinne des Browsers: kein `role="dialog"`, kein `aria-modal`, keine Escape-Taste, kein Fokuskäfig. Das Hilfe-Sheet daneben hat alles davon. | `public/index.html:1289` (`.ov`) |

| ui-designer | 2 | Das Hilfe-Sheet öffnet sich beim ERSTEN Besuch eines Schritts von selbst und legt sich über den ganzen Schirm — samt Statuszeile. Wer mit einem überholten Stand oder „Nicht angemeldet" in einen Schritt geht, sieht zuerst eine Bedienungsanleitung und die Fehlermeldung erst danach. Im Browser gesehen (`review/screens/runde-2/`, erster Lauf). Entweder das Sheet nicht selbständig öffnen, wenn eine Fehlermeldung ansteht, oder die Statuszeile über dem Sheet halten. | `public/index.html:1697` (`netzChip`), Hilfe-Sheet `#hilfe` |
| ui-designer | 2 | Der überholte Stand lässt sich jetzt sauber quittieren — damit ist der Punkt des software-engineers („niemand kann den Stand ANSEHEN") dringender, nicht kleiner: Der Knopf „Gesehen" ist die einzige Handlung, und danach ist der Stand endgültig aus dem Blick. Die Ansicht „Was war in meinem Stand?" gehört daneben, dann heißt der zweite Knopf „Ansehen". | `public/index.html:1560` (`insSackfach`), `:1681` (`ueberholtSatz`) |

| qa-guardian | 2 | „Nicht angemeldet – bitte neu anmelden" steht seit Runde 2 auch **im laufenden Schritt** — dort gibt es aber nichts zum Anmelden. `bWechseln` wird ausschließlich in `#menu` gerendert; der Weg ist „Menü" oben rechts, dann „wechseln". Der Zählstand überlebt das (`hh_keller_v12` hängt nicht an der Sitzung), aber der Satz sagt, was zu tun ist, und zeigt nicht, wo. Im Persona-Durchlauf gemessen (`review/screens/runde-2-qa/10-sitzung-abgelaufen.png`). | `public/index.html:1744` (Satz), `:1980` (`bWechseln`, nur im Menü) |
| qa-guardian | 2 | Das Sackfach schneidet still auf 20 Einträge (`a.slice(-20)`): Der einundzwanzigste überholte Stand verdrängt den ältesten, ohne dass irgendwo etwas davon steht. Die Obergrenze ist richtig (der Gerätespeicher darf nicht zulaufen), das stille Verwerfen ist die Kehrseite von Regel 6. Mindestens: den Verlust in der Zeile nennen. Prüfung hält es fest: `tests/ausgang.test.mjs` „das Sackfach fasst 20 Stände". | `public/index.html:1586` (`insSackfach`) |

| software-engineer | 3 | `vorgang.wer` und `ereignis.wer` bekommen `daten.name || p.name` – also zuerst den Namen, den der Client mitschickt, und erst ersatzweise den der angemeldeten Person. Im Journal steht damit ein Name, den der Absender frei wählen kann, obwohl die Sitzung die Person kennt. Solange nur das Haus Zugriff hat, ist es kein Loch, aber es ist die falsche Reihenfolge: `p.name` sollte gelten, `daten.name` höchstens danebenstehen. Nicht in Runde 3 geändert, weil es das Verhalten der App berührt (sie schickt den Namen des Geräts mit). | `src/index.js:211` (`vorgangSchreiben`), `:296` (`schreibe`) |
| software-engineer | 3 | `werkzeug/abgleich.js` ist toter Code und läuft nicht: es führt `gnParse`, `gnErkennt`, `gnMap` und `verbrauch` ein – keine dieser vier gibt es in `src/gnparse.js` oder `src/gnmap.js` (dort stehen `parseZ`, `zahl`, `ml`, `felder`, `kern`, `mappe`, `WEINE`). Der erste Aufruf wirft beim Einlesen. Entweder auf die heutigen Namen bringen oder entfernen. Wert hat die Datei trotzdem: sie ist der einzige Ort, der erklärt, wofür `fassungsliste.kostenstelle` und `z` gedacht waren. | `werkzeug/abgleich.js:12` |
| software-engineer | 3 | `vorgang.branch` wird nirgends geschrieben und nirgends gelesen. Der Name passt zum Architekturpunkt „zwei Geräte am selben Vorgang: fragen, nie still zusammenführen" – heute löst das die `zaehlnr` im JSON plus der 409. Entweder gehört `branch` dazu oder in dieselbe Schublade wie `schluessel`/`zaehlnr`/`geraet` (Regel 14). Bitte einmal festlegen. | `docs/live-schema.sql:44` |

| ui-designer | 3 | **`table{min-width:560px}`-Falle Nr. 8 sitzt jetzt woanders: die Befund-Pille.** In „Verkauf ↔ Fassung" bekommt ohne Z-Bericht jede Zeile die rote Pille „prüfen", obwohl der Hinweis darüber sagt, dass nur die Entnahme dasteht. Der Mittagsblick ist in Runde 3 berichtigt (Kachel und Navigationszahl zeigen „—"), die Tabelle selbst nicht — sie wäre die dritte Stelle mit derselben Regel und gehört mit `zeile()` gemeinsam angefasst. | `public/leitung.html:1232` (`vAbgleich`, `zeile`) |
| ui-designer | 3 | **`.b.klein` ist 28 px hoch** (`--control-h-sm` bei `data-dichte="maus"`), im Backoffice elfmal im Einsatz — „Als CSV", „Jetzt zuordnen", „Warum so gerechnet?". Die Kopfleiste ist in Runde 3 auf 36/44 px gebracht, die kleinen Knöpfe nicht: das hieße, `--control-h-sm` anzufassen, und das Token steht in der GETEILTEN Schicht (Falle 1) — es würde auch jeden kleinen Knopf im Keller verändern. Braucht eine Entscheidung, keinen stillen Griff. Gemessen im Browser. | `public/leitung.html:412` (`.b.klein`), geteilter Block `:110` (`--control-h-sm`) |
| ui-designer | 3 | **Die Leitung liest den Gerätespeicher nicht mehr, sobald der Server antwortet.** `ladeDaten()` steigt bei `r.ok` aus, auch wenn die Liste leer ist — richtig so, aber es heißt: ein MacBook, auf dem noch ein Archiv der Fassungsseite liegt, zeigt es ab sofort nicht mehr an. Vor Runde 3 war das der Normalfall (der Server warf immer einen Fehler). Wer alte Stände braucht, muss sie über „Vorgänge aus Datei laden" hereinholen — das steht nirgends. | `public/leitung.html:697` (`ladeDaten`), `:1576` (`vEinst`, Dateiknopf) |

| hospitality-pro | 3 | **Ein zweiter Z-Bericht für denselben Tag ersetzt den ersten still.** Im Betrieb sind das zwei verschiedene Fälle: ein zweiter Abschluss nach Nachbuchung oder Storno (dann ist Ersetzen richtig) und ein getrennter Abschluss von Bar und Restaurant (dann geht eine ganze Kostenstelle verloren). Ohne Schemaänderung lösbar: Die Notiz im Journal soll beim Ersetzen alte und neue Z-Nummer und beide Positionszahlen nennen — fällt die Zahl der Positionen deutlich, war es ein Teilbericht. Antwort zu Entscheidung Nr. 11. | `src/index.js` (`fassungsliste`), `review/OFFENE-ENTSCHEIDUNGEN.md` Nr. 11 |
| hospitality-pro | 3 | Der Knopf in der Quellzeile heißt jetzt je nach Lage anders („Erneut versuchen“ / „Nochmal nachsehen“ / „Aktualisieren“). In der Kopfleiste steht daneben weiter „Aktualisieren“ — zwei Knöpfe, ein Ergebnis. Das ist kein Fehler, aber beim nächsten Anfassen der Kopfleiste zusammendenken. | `public/leitung.html:782` (`quellBanner`), `:1945` (Kopfleiste) |
| qa-guardian | 3 | **Der Z-Import ist nicht atomar.** `fassungsliste()` setzt drei Schreibvorgänge hintereinander ab: Kopf einfügen (`.run()`), alte Zeilen löschen (`.run()`), neue Zeilen als `batch`. Scheitert der `batch` — eine zu lange Bezeichnung, ein Typ, den D1 ablehnt —, dann steht der Kopf mit frischem `importiert` da und **keine einzige Position**. Genau das Auseinanderlaufen, das `vorgangSchreiben` in Runde 3 durch ein gemeinsames `batch` schon abgestellt hat; hier steht es noch. Im Mailweg fällt es niemandem auf, weil alles im `waitUntil` läuft. | `src/index.js:397`–`:422` |
| qa-guardian | 3 | **Nach dem Livegang ist der Gerätespeicher der Leitung nirgends mehr erreichbar.** Gemessen (`tests/ui-leitung.cjs`, neue Lage „leerarchiv"): Server antwortet `200` mit leerer Liste → `QUELLE="Server"`, **3 Vorgänge im `hh_archiv`, 0 gezeigt**. Auch die Seite „Speicher" hilft nicht, sie liest nur `VORGAENGE`. Der einzige Weg zurück ist „Datei laden" mit einer JSON-Ausfuhr, die es vielleicht gar nicht gibt. Nichts ist verloren, aber es ist unsichtbar — und die Quellzeile „Leer" erwähnt den eigenen Browserspeicher nicht. Schärfung des Punktes des ui-designers aus derselben Runde. | `public/leitung.html:698`–`:718`, `:1667` (`vSpeicher`) |
| qa-guardian | 3 | **Im Schritt „Holen" führt „Weiter" weiter, ohne dass etwas abgehakt ist.** Im Persona-Durchlauf drückt die neue Servicekraft unten „Weiter", kommt in den Abschluss und liest dort zum ersten Mal „Noch nicht alles geholt – 3 Weine". Im Schritt selbst steht kein Wort darüber, dass jede Zeile einzeln anzutippen ist; einen Sammelknopf wie `.fullbtn` in Bar und Restaurant gibt es hier nicht. Sie muss zurück und den Schritt neu verstehen. | `public/index.html:2668` (`rHolen`), `:2690` (`holRow`), `:4048` (`offenList`) |

## Niedrig

| Rolle | Runde | Punkt | Datei:Zeile |
|---|---|---|---|
| qa-guardian | 16 | **Unerreichbarer Zweig in `popupAbgleich()`.** Der dritte `warum`-Zweig („gibt es nichts, was zu dieser Fassung passt") setzt `geprueft===0` bei `ohneGebinde=0`, `ohneMenge=0` und `ohneZuordnung=0` voraus. Dann wäre aber jede Position gerechnet, also `verk` nicht leer und `geprueft>0`. Der Satz kann nie erscheinen. Harmlos, aber er täuscht eine geprüfte Lage vor. | `public/index.html` (`popupAbgleich`, `warum`) |
| qa-guardian | 16 | **`zahl()` lässt negative Stückzahlen durch** (`parseFloat`), eine Retourenzeile im Positionsblock ergäbe „verkauft −n". App (`verkaufteFlaschen`) und Backoffice (`flaschen`) rechnen gleich falsch, deshalb kein Widerspruch zwischen den Schirmen — im Bericht 37 kommt der Fall nicht vor. | `src/gnparse.js:18` |
| qa-guardian | 16 | **`POST /api/person/pin` nimmt `code` auch als Zahl.** `PIN_MUSTER.test(1234)` ist wahr (die Prüfung wandelt selbst um), `hashe()` ebenso — gespeichert wird das Richtige, zurück kommt aber `"pin": 1234` statt Text. Eine führende Null ginge so verloren. `typeof mit !== "string"` abweisen. | `src/index.js:790` |
| qa-guardian | 16 | **`review/OFFENE-ENTSCHEIDUNGEN.md` nennt `"code":"1234"` als Beispiel für `POST /api/anlage`.** Kein echter Code des Hauses, aber ein Muster, das jemand eins zu eins abtippt. Besser `"code":"NNNN"`. | `review/OFFENE-ENTSCHEIDUNGEN.md` |
| ui-designer | 9 | Bei sechs Flaschen je Position bricht die Punktreihe 5+1 statt 4+2 (im Plan kommt kein Wein mit mehr als 3 vor). | `public/index.html` (`.dots{max-width}`) |
| ui-designer | 9 | `--text-xs` und `--text-sm` sind für den Finger beide 15 px; die Rangfolge trägt jetzt Gewicht und Laufweite. Wer im Service einen kleineren Grad will, muss bei 15 px bleiben. | geteilte Gestaltungsschicht |
| ui-designer | 9 | Das Backoffice hat unter 900 px Fingergeometrie, aber weiter 11/13-px-Schrift. Falls die Leitung je am Handy arbeitet, ist das zu entscheiden. | `[data-dichte="maus"]` im `@media(max-width:900px)` |
| Jäger | 8 | Worker und Backoffice sind sich bei gleichem Betriebstag und verschiedener Ankunft **einig und beide falsch**; sichtbar wird der Unterschied heute nicht, weil `/api/bestand` außer den Prüfungen keinen Leser hat. Nach Runde 10 gegenprüfen. | `src/index.js` (`bestand`) |
| Jäger | 6 | Das Messgerät zählt Kinder eines `.vh`-Behälters als sichtbare Trefferflächen — „Zurück"/„Weiter" 65 × 29 auf drei Seiten sind Geisterfunde, die gemeldete Zahl 96 ist zu hoch. `sicht()` prüft die Klasse nur am Element selbst. | `tests/ui-mass.cjs:99` |
| Jäger | 6 | `.vh` steht zweimal in `index.html` (einmal in der Gestaltungsschicht, einmal darunter) — zwei identische Regeln. | `public/index.html:221`, `:761` |
| Jäger | 6 | `parseZ` liefert `p.ml` bereits mit; `flaschen()` rechnet dieselbe Regel über `mlAusText(p.name)` ein drittes Mal. Heute deckungsgleich (15 Proben, 0 Abweichungen), aber ein drittes Vorkommen derselben Regel. | `src/gnparse.js`, `public/leitung.html:1066` |
| software-engineer | 7 | `PLAN.kiste` wird nur noch in der Bestellliste benutzt; klären, ob die Kistengröße je Artikel überhaupt Stammdaten sein soll. | `public/leitung.html` (Bestellen) |


| Rolle | Runde | Punkt | Datei:Zeile |
|---|---|---|---|
| hospitality-pro | 1 | Kistengröße für Ott (w003) ist mit 12 angenommen, nicht geprüft (Projektanleitung §8). Beim nächsten Wareneingang am Lieferschein nachsehen – eine falsche Kistengröße verdoppelt oder halbiert den Zugang still. | `src/stamm.json` |
| software-engineer | 1 | `versuche: 0` im Ausgangseintrag wird nie hochgezählt und nie gelesen – toter Code. Entweder Zählung führen und im Netz-Chip zeigen oder Feld streichen. | `public/index.html:1514` (`inDenAusgang`) |
| software-engineer | 1 | Verwaltung und Freigabe hängen jetzt an `hh_bekannt_v1`. Wird der Speicher des Geräts geleert, sind beide bis zur nächsten Anmeldung mit Netz gesperrt. Im Keller ohne Netz fällt das auf. | `public/index.html:1330` (`bekannterCode`) |
| ui-designer | 1 | `--danger` (#B4471F) als Textfarbe auf Cream ergibt 4,4:1 und liegt damit unter dem Schwellwert für kleine Schrift. `.pinfehler` ist umgestellt, andere Stellen (`.mi .when`, `.explesser button.del`) nicht — die sind 11–13px groß. | `public/index.html:360` (`.mi .when`), `:314` (`.explesser button.del`) |
| qa-guardian | 1 | `inDenAusgang(rec, status)` nimmt einen zweiten Parameter entgegen und benutzt ihn nie – alle fünf Aufrufstellen übergeben „laeuft" oder „fertig". Der Zustand kommt tatsächlich aus `daten.finished`, das der Worker beim Lesen darüberlegt. Entweder verwenden oder streichen. | `public/index.html:1500` (`inDenAusgang`) |
| qa-guardian | 1 | Die Prüfungen unter `tests/` decken Anmeldung, Ausgang, Vorgänge und den Z-Bericht ab; die Oberfläche selbst ist nach wie vor ungeprüft (kein Browser, kein Playwright, keine `node_modules` in dieser Umgebung). Der Persona-Lauf „neue Servicekraft, erster Tag" steht weiter aus. | `tests/` |
| ui-designer | 1 | Der Zustand „Nicht angemeldet" ist nicht anfassbar: die Statuszeile sagt, was zu tun ist, führt aber nicht hin. Ein Knopf „Neu anmelden" wäre der kurze Weg. | `public/index.html:923` (`.netz`), `:1604` (`netzChip`) |

| ui-designer | 2 | `netzChip()` heißt weiter so, ist aber seit Runde 1 kein Chip mehr und malt seit Runde 2 an zwei Stellen. Das Element trägt zudem noch `id="netz"`, obwohl kein Selektor es mehr benutzt — gesucht wird jetzt über die Klasse. Beim nächsten Anfassen aufräumen (`netzZeile()`?). | `public/index.html:1697` (`netzChip`), `:1317` (`id="netz"`) |
| ui-designer | 2 | Im Menü steht `--topH` auf `0px`: `measure()` misst `#topwrap`, das dort in einem Elternteil mit `display:none` liegt. Heute folgenlos (die Variable hängt nur an `.grpanchor{scroll-margin-top}` innerhalb des Vorgangs), aber eine Falle für jeden, der `--topH` künftig außerhalb von `#app` benutzt. Im Browser gemessen. | `public/index.html:510` (`.grpanchor`), `:4419` (`measure`) |

| ui-designer | 3 | „Nichts gefunden." steht im Speicher auch dann, wenn es überhaupt keine Vorgänge gibt — in einer Ansicht mit Suchfeld liest sich das als „dein Filter passt auf nichts", nicht als „es gibt nichts". Zwei Fälle, zwei Sätze. | `public/leitung.html:1705` (`vSpeicher`, `malSpeicher`) |
| ui-designer | 3 | Das Navigationsblatt unter 900 px hat keinen sichtbaren Schließen-Knopf; es geht mit Escape, mit einem Tipp daneben oder über denselben Hamburger. Das ist entdeckbar (der Grund ist abgedunkelt), aber ein „×" oben im Blatt wäre der kurze Weg. | `public/leitung.html:498` (`#bNav`), `:466` (`body.navoffen nav.seite`) |

| hospitality-pro | 3 | `kern()` schneidet nur die Maßangabe ab. In der Kasse heißen Positionen aber auch „… Glas“, „… Fl.“, „… Karaffe“ — dann bekämen „Zweigelt Glas“ und „Zweigelt 0,75 l“ zwei verschiedene `kern` und fänden in der Leitung nicht zusammen. Nicht auf Verdacht erweitern: am ersten echten Z-Bericht ablesen, dann eine Zeile ändern. | `src/gnparse.js:66` (`kern`) |
| qa-guardian | 3 | Scheitert der Z-Import mit `422` („kein Betriebstag erkannt"), schreibt der Mailweg trotzdem eine Erfolgsnotiz ins Journal: `j.tag` ist dann `undefined`, und im Journal steht „Z-Bericht undefined: undefined Positionen, undefined offen". Der Fehlertext aus der Antwort geht verloren. | `src/index.js:645`–`:646` |
| qa-guardian | 3 | In den Prüfungen stehen erfundene vier- bis sechsstellige Ziffernfolgen fest im Quelltext (`418823`, `770231`, `112233`, `000000`, `4711`). Sie sind erfunden, aber niemand kann ausschliessen, dass eine davon zufällig mit einem Code aus dem Haus zusammenfällt — und in der Geschichte stehen sie dann für immer. `tests/durchstich.cjs` würfelt seinen Code seit Runde 3 beim Start; die übrigen Prüfungen könnten dasselbe tun. | `tests/worker-anmeldung.test.mjs`, `tests/persona-tagesfassung.cjs:52` |

## Erledigt

| Rolle | Runde | Punkt | Erledigt in |
|---|---|---|---|
| hospitality-pro → Hauptsitzung | 5 | **Das Backoffice hatte einen ZWEITEN Z-Bericht-Leser** (`zParse`, `mlAusText`) mit genau den Fehlern, die in `gnparse.js` schon behoben waren. Dieselbe Datei: Worker 48 Positionen / 602,50 €, Backoffice 106 / 5166,70 €, das Achtel als acht Liter. Der zweite Leser ist ersatzlos weg; die Seite schickt den Bericht an `POST /api/fassungsliste` und holt ihn zurück. | Runde 5, `public/leitung.html` (Abschnitt 5), `tests/ui-leitung-echt.cjs` |
| hospitality-pro → Hauptsitzung | 5 | **Z-Berichte und Zuordnungen lagen im `localStorage` eines Geräts.** Der per Mail eingelieferte Bericht war im Backoffice unsichtbar, die Zuordnungsarbeit hing an einem Browser, und der Mail-Import las eine Zuordnungstabelle, in die niemand schrieb. Beides kommt jetzt vom Server (`/api/fassungsliste`, `/api/mapping`), der `localStorage` ist nur noch Abschrift. | Runde 5, `public/leitung.html` (`ladeBerichte`, `ladeZuordnung`, `sendeZuordnung`) |
| software-engineer → Hauptsitzung | 5 | **Der Wareneingang buchte jede Kiste mit sechs Flaschen.** Der Worker las `x.kg`, die App schreibt `kistengr` — beim Zwölfer kam die halbe Lieferung an, während der Schirm „24 Flaschen · 12er Kisten" zeigte. | Runde 5, `src/index.js` (`ereignisseAbleiten`), `tests/modi.test.mjs` |
| software-engineer → Hauptsitzung | 5 | **Eine Getränkelieferung wurde als Entnahme gebucht.** `gent` heisst im Wareneingang „geliefert", in der Sonderentnahme „geholt"; beides lief durch dieselbe Zeile. Vorzeichenfehler von 48 Flaschen je Lieferung. | Runde 5, `src/index.js` (`ereignisseAbleiten`) |
| software-engineer → Hauptsitzung | 5 | **Nachfüllen erzeugte keine einzige Journalzeile.** Der Worker summierte die Weinorte, die beim Nachfüllen leer sind; die geholten Getränke standen in `gholt`/`gholtN`, die er nie las. Die App schreibt sie jetzt beim Abschluss nach `gent`. | Runde 5, `public/index.html` (`geholteGetraenke`), `src/index.js` |
| software-engineer → Hauptsitzung | 5 | **„Ignoriert" räumte den Artikel nicht aus schon eingelesenen Zeilen** — „Verkauf ↔ Fassung" rechnete weiter mit einer Position, die die Leitung gerade ausgeschlossen hatte. | Runde 5, `src/index.js` (`mappingSchreiben`) |
| software-engineer → Hauptsitzung | 5 | **Die Absenderprüfung des Postfachs prüfte das Ende der ganzen Adresse.** `post@boesegastronovi.com` kam durch, solange `gastronovi.com` freigegeben war — und legte echte Z-Berichte in die Datenbank. Jetzt wird die Domäne verglichen. | Runde 5, `src/index.js` (`email`) |
| software-engineer → Hauptsitzung | 5 | **Ein abgelehnter Mailimport meldete „Z-Bericht undefined: undefined Positionen".** Die Notiz nennt jetzt Status, Grund und Betreff. | Runde 5, `src/index.js` (`email`) |
| hospitality-pro → Hauptsitzung | 5 | **Eine Teilzählung setzte 54 von 57 Weinen auf „—".** Das Backoffice suchte EINEN Basis-Vorgang; wer die eigene Zählliste benutzte, zerschoss sich den Bestand. Jetzt je Artikel die jüngste Zählung, wie im Worker. | Runde 5, `public/leitung.html` (`bestand`), `tests/ui-leitung-echt.cjs` |
| hospitality-pro → Hauptsitzung | 5 | **Die Leitung konnte sich mit einem Klick selbst aussperren** — bei einer einzigen Leitungsperson kam danach niemand mehr hinein, Rückweg nur über die D1-Konsole. Beides wird jetzt abgefangen, und ein 401 heisst nicht mehr „Der Server antwortet nicht". | Runde 5, `public/leitung.html` (`vTeam`) |
| Betreiber → Hauptsitzung | 1→4 | **`tests/fixtures/` ist gefüllt.** Der erste echte, anonymisierte Z-Bericht (Nr. 37, Nacht 15./16.09.) liegt im Repo. Damit ist die grösste Unsicherheit des Projekts weg — vierfach angemahnt, in Runde 4 geliefert. | Runde 4, `tests/fixtures/zbericht-37-extended.csv` |
| Hauptsitzung | 4 | **`parseZ` erkannte im echten Bericht keine einzige Sektion.** Gastronovi eröffnet jede Tabelle mit einer Spaltenüberschrift („Positionen \| Anzahl \| Betrag"), nicht mit einem nackten Titel; dazu Rauten statt Strichen zwischen den drei Teilen, die Z-Nummer in zwei Feldern und Leerzeilen aus vier leeren Feldern. Gelesen wurden dadurch 106 „Positionen" mit 1345,75 Stück und 5166,70 € — Steuersätze, Kellner und Warengruppen als Getränke. Jetzt 48 Positionen, 145 Stück, 602,50 €, gegengerechnet an zwei Summen des Berichts. | Runde 4, `src/gnparse.js:96`, `:124`, `:139` |
| Hauptsitzung | 4 | **„1/8 l" wurde als acht Liter gelesen** (Faktor 64 auf jedem offenen Wein): Die Einheitensuche griff vor dem Bruch. Derselbe Fehler im Kern liess „GV Leindl Langenlois 1/" stehen, sodass Glas und Flasche desselben Weins nie zusammenfanden. Beides behoben, mit den Namen aus dem echten Bericht geprüft. | Runde 4, `src/gnparse.js:40` (`ml`), `:69` (`kern`) |
| Hauptsitzung | 4 | **Rabatt und Storno waren im Ergebnis nicht sichtbar.** Beides ist Verbrauch (Vorgabe des Betreibers). Der Rabatt steckt schon in den Positionen (602,50 − 52,00 = 550,50, auf den Cent nachgerechnet), der Storno fehlt ihnen und nennt keinen Artikel. `parseZ` gibt jetzt beides heraus, der Import meldet den Storno, und die Journalnotiz der Mail nennt ihn. | Runde 4, `src/gnparse.js:174`, `src/index.js:424`, `:645` |
| Hauptsitzung | 4 | **Kein Codefeld nahm mehr als vier Ziffern** (`maxlength="4"` in Anmeldung, Verwaltung und Freigabe), und die Anmeldung schickte den Code bei der vierten Ziffer von selbst ab. Auflage 2 vor dem Livegang („neue Codes, nicht wieder vierstellig") hätte damit das ganze Haus ausgesperrt — ohne Fehlermeldung, mit Sperre nach zehn Versuchen. Jetzt sechs bis acht Ziffern, abgeschickt auf eine Bestätigungstaste (✓). | Runde 4, `public/index.html:1861` (`renderLogin`), `:2094` (`askPin`), `:4026` (`freigabe`) |
| Hauptsitzung | 4 | **Die Löschtaste des Ziffernblocks hängte „undefined" an den Code.** `data-weg` steht ohne Wert in der Zeile, `b.dataset.weg` ist damit `""` und jeder Wahrheitstest darauf falsch — der Tastendruck fiel in den Zweig für Ziffern und hängte `undefined` an. Wer sich im Keller vertippte, kam nicht mehr herein. Geprüft wird jetzt `"weg" in b.dataset`; `tests/durchstich.cjs` drückt die Taste. | Runde 4, `public/index.html:1899` |
| Hauptsitzung | 4 | **Ein ersetzter Code blieb auf dem Gerät gültig.** `merkeCode` legte je Code einen Eintrag ab und nahm keinen weg; der alte kam über die Rückfallebene ohne Netz weiter in die App und öffnete Verwaltung und Freigabe. Ein Gerät merkt sich jetzt je Person genau einen Code — den zuletzt beim Server bestätigten. Neue Prüfung `tests/geraetecodes.test.mjs`. | Runde 4, `public/index.html:1369` |
| Hauptsitzung | 4 | **Die Taste „Vorschlagen" im Backoffice schlug vierstellige Codes vor** — also genau das, was Auflage 2 verbietet. Jetzt sechsstellig und ohne Rest-Schieflage (Verwerfen statt Modulo über die letzte volle Runde). | Runde 4, `public/leitung.html:1873` |
| qa-guardian → Hauptsitzung | 3→4 | Erfundene Ziffernfolgen standen fest im Quelltext der Prüfungen. `tests/worker-anmeldung.test.mjs`, `tests/persona-tagesfassung.cjs` und die neue `tests/geraetecodes.test.mjs` würfeln ihre Codes jetzt beim Start, wie `tests/durchstich.cjs` seit Runde 3. | Runde 4 |
| hospitality-pro | 3 | „Ungeprüft gilt als voll“ steht jetzt als erster Eintrag in der Bedienungshilfe, mit der Folge dahinter („fehlt darum am nächsten Abend im Service“). Vorher nur in zwei Leermeldungen im Keller. | Runde 3, `public/index.html:2268` |
| hospitality-pro | 3 | Ein Wort für den Gang nach unten: Der Schritt der Tagesfassung heißt nicht mehr „Keller“, sondern wie im Nachfüllen „Holen“ — gleich der Hilfe („Holen und einräumen“) und der Abschlussmeldung („Noch nicht alles geholt“). | Runde 3, `public/index.html:1392`, `:1412` |
| ui-designer | 3 | **Der leere Stand log.** Bei null Vorgängen zeigte der Mittagsblick zwei grüne Kacheln: „Auffällige Differenzen 0 — Verkauf und Entnahme decken sich" und „Nachbestellen 0 — nichts unter der Schwelle". Beides war strukturell unmöglich: ohne Z-Bericht ist nichts vergleichbar, und ohne Kellerzählung kann `bestellliste()` gar nichts finden (§4.3). Am ersten Tag nach dem Livegang wäre das der erste Schirm gewesen. Fehlt die Grundlage, steht jetzt „—" mit dem Grund; die rote Zahl in der Navigation verschwindet ebenfalls. | `public/leitung.html:1113` (Kacheln), `:1033` (`zaehler`) |
| ui-designer | 3 | **Die Quelle war ein Punkt in der Ecke — und unter 900 px gar nichts.** „Lokal" und „Nicht angemeldet" bekamen denselben grünen Punkt wie „Server"; `.kopf .chip{display:none}` blendet den Chip am Handy ganz aus. Jetzt sagt eine Quellzeile über dem Inhalt in Worten, was los ist und was zu tun ist, auf jeder Breite und in allen zwölf Ansichten; der Punkt wird bei Warnung und Fehler zum Ausrufezeichen (gemessen: `--danger` auf der Teal-Leiste 1,89:1, `--fg-on-dark` 10,27:1). | `public/leitung.html:345` (`.quellzeile`), `:749` (`QUELLLAGE`), `:1945` (`zeichne`) |
| ui-designer | 3 | **Der einzige Ausweg aus der abgelaufenen Sitzung war ein blauer Browserlink.** Die Tür rendert `<a class="b stark">`, die Regel hieß aber `button.b` — ein Typselektor, der auf ein `<a>` nicht greift. Der Knopf „Zum Fassungstool" stand ungestylt und rund 20 px hoch da. Selektor auf die Klasse gebracht: gemessen 44 px am iPhone, 36 px am MacBook. | `public/leitung.html:404` (`.b`), `:689` (`verlangeAnmeldung`) |
| ui-designer | 3 | Trefferflächen der Kopfleiste: „Aktualisieren" und „Fassungstool ›" waren fest 30 px hoch, der Navigationsknopf 36 px — auch am iPhone, wo 44 px gelten. Beide hängen jetzt an `--control-h` (36 px bei `data-dichte="maus"`, 44 px unter 900 px). Im Browser nachgemessen, vorher und nachher. | `public/leitung.html:264` (`.kopf button.k`), `:270` (`.navknopf`) |
| Setup → Betreiber | 2→3 | `docs/live-schema.sql` fehlte im Repo; ohne die Datei war Regel 3 nicht erfüllbar. Der Betreiber hat sie aus der laufenden D1 gezogen. Sie ist seit Runde 3 die Grundlage aller Prüfungen: `tests/hilfe/d1-echt.mjs` baut daraus eine echte SQLite-Datenbank. | `docs/live-schema.sql` |
| qa-guardian → software-engineer | 2→3 | **`vorgang.art`, `vorgang.person`, `vorgang.ts` gibt es live nicht, `modus`/`begonnen`/`status` sind NOT NULL und wurden nicht geschrieben – es ist nie ein Vorgang angekommen.** Dieselbe Abweichung in `fassungsliste`, `fassungszeile`, `mapping`, `stamm` und in `notiz()`. Aller Worker-SQL ist an `docs/live-schema.sql` angepasst; nachgewiesen an echtem SQLite mit dem Live-Schema. | `src/index.js` (alle SQL-Stellen), `tests/live-schema-durchlauf.test.mjs` |
| qa-guardian | 2→3 | `quelle='vorgang-korrektur'` war gegen das Live-Schema ungeprüft. Ergebnis: `ereignis.quelle` hat **keine** CHECK-Bedingung, `ereignis.art` schon (`zaehlung|entnahme|eingang|korrektur`). Die Gegenbuchung ist zulässig und wird jetzt gegen die echte Tabelle geprüft. | `src/index.js:229` (`SQL_EREIGNIS_KORR`), `tests/live-schema-durchlauf.test.mjs` |
| qa-guardian | 2→3 | `vorgangSchreiben` schrieb die `vorgang`-Zeile und leitete danach die Ereignisse ab – zwei `run()` sind in D1 keine Transaktion. Beides geht jetzt in **ein** `batch`, das D1 als Ganzes zurücknimmt; die Prüfung erzwingt den Fehler im Journal und weist nach, dass danach auch der Vorgang nicht dasteht. | `src/index.js:204–224`, `:293` (`schreibe`) |
| software-engineer | 2→3 | Die D1-Attrappe (`tests/hilfe/d1-attrappe.mjs`) kannte genau die falschen Spaltennamen und hat sie damit bestätigt: 123 grüne Prüfungen über einem Worker, der live keine einzige Zeile schreiben konnte. Ersetzt durch `tests/hilfe/d1-echt.mjs` – SQLite aus `docs/live-schema.sql`, Bordmittel von Node 22, keine neue Abhängigkeit. | `tests/hilfe/d1-echt.mjs` |
| qa-guardian → software-engineer | 1→2 | Korrektur nach dem Abschluss erreichte den Bestand nie: `ereignisseAbleiten` stieg aus, sobald Ereignisse da waren. Jetzt entscheidet der INHALT, nicht die Anwesenheit: gebucht wird die Differenz zum bereits Gebuchten, als neue Zeile mit `quelle='vorgang-korrektur'`. Doppeltes Senden bucht weiterhin nichts, auch mit neuer Zählnummer. | `src/index.js:195–313` (`ereignisseAbleiten`), `tests/worker-vorgang.test.mjs` |
| qa-guardian → software-engineer | 1→2 | Nach einem 409 verschwand der eigene Stand und die Zeile meldete Vollzug. Er verlässt jetzt nur die Warteschlange (weiter senden wäre sinnlos und hielte die Reihe auf) und wandert ins Sackfach `hh_ueberholt_v1`; die Statuszeile nennt ihn mit Uhrzeit und, wenn der Server ihn mitschickt, mit dem Namen des schnelleren Standes. | `public/index.html:1519–1546` (Sackfach), `:1669` (`netzChip`), `tests/ausgang.test.mjs` |
| software-engineer | 2 | Im Router standen acht Handler-Aufrufe ohne `await` im `try`. Ein Datenbankfehler platzte damit erst nach dem `try`: kein `console.error`, keine 500 mit Klartext, sondern ein nacktes 1101 (Falle 10). Alle acht abgewartet, fünf Prüfungen dazu. | `src/index.js:461`, `:464`, `:476`, `:477`, `:484`, `:488`, `:491`, `:504`, `:517` · `tests/worker-fehler.test.mjs` |
| software-engineer | 2 | Die D1-Attrappe kannte die Abfrage von `bestand()` nicht – `/api/bestand` lief in keiner einzigen der 78 Prüfungen. Handler ergänzt; Einfügungen ins Journal werden jetzt aus Spalten- und VALUES-Liste gelesen statt nach Position geraten. | `tests/hilfe/d1-attrappe.mjs` |
| software-engineer | 2 | `tests/worker-anmeldung.test.mjs` prüfte „der nächste Vertipper fängt wieder bei neun an" mit einer immer wahren Bedingung, die selbst einen Fehlversuch absetzte – geprüft wurde die 8 des zweiten Versuchs. Ein Versuch, eine Zahl, jetzt 9. | `tests/worker-anmeldung.test.mjs:86` |
| ui-designer | 1→2 | Im Menü gab es keinen Übertragungszustand: `#netz` lag in `#app` und war nur während eines Vorgangs sichtbar. Nach dem Abschluss — genau dann, wenn die Frage „ist es draußen?" kommt, und genau dort, wo die 409-Meldung aus Runde 2 eintrifft — war die Zeile blind. `netzChip()` malt jetzt an alle `.netz`; im Menü steht derselbe Zustand als Block über den Kacheln. | `public/index.html:1697` (`netzChip`), `:1946` (Block im Menü), `:972` (`.netz--block`) |
| software-engineer → ui-designer | 2→2 | Die Meldung zum überholten Stand war ein Satz mit 113 Zeichen und einer erfundenen Geste („Tippen, wenn gesehen") an einem `role="status"` mit `onclick`, ohne Fokus, ohne Tastatur, ohne Rolle. Jetzt 66 Zeichen und ein echter `<button>` daneben; die Live-Region sitzt auf dem Text, nicht auf dem Knopf. | `public/index.html:1681` (`ueberholtSatz`), `:1317`, `:1946` (Markup), `:948` (`.netzok`) |
| ui-designer | 1→2 | Die Trefferfläche des „?"-Knopfes war 42px, nicht die in Runde 1 behaupteten 44px: `inset:-4px` an einem `::after` rechnet gegen die Polsterkante, der 1px-Rahmen fällt heraus. Im Browser nachgemessen und berichtigt; „Menü" (ohne Rahmen) war korrekt. | `public/index.html:398` (`.home::after,.hilfebtn::after`) |
| Setup | 1 | Verwaltungscode `const PIN` im Klartext in der ausgelieferten App (Regel 9). Konstante und Erwähnung im Kommentar entfernt; Verwaltung prüft jetzt `bekannterCode()`. | `public/index.html:1889` (Kommentar), `:1899` (`askPin`) |
| Setup | 1 | Abschluss-Freigabe über den Klartext-Code übersteuerbar. Es zählt nur noch ein Zugangscode, der auf diesem Gerät schon einmal angemeldet war. | `public/index.html:3798` (`freigabe`) |
| software-engineer | 1 | Anmeldesperre nach §9.1 C: 10 Versuche, `uebrig` bei 401, `wartenBis` bei 429, Erfolg räumt die Fehlversuche der IP weg. | `src/index.js:28`, `:101`, `:127`, `:136` |
| qa-guardian | 1 | `npm run schema` führte `wrangler d1 execute fassung --remote --file=./schema.sql` aus – ein Befehl gegen die Live-Datenbank mit genau der Datei, die laut Regel 3 nie ausgeführt werden darf. Skript entfernt, dafür `npm test`. | `package.json` |
| qa-guardian | 1 | Beide Gestaltungsblöcke trugen die Anleitung „Einbinden als ERSTES Stylesheet: `<link rel=stylesheet href=tokens.css>`". Wer ihr folgt, verweist auf eine Datei, die es nicht mehr gibt – die Seite steht dann nackt da (Projektanleitung §10). Kommentar in beiden Dateien wortgleich ersetzt, `sw.js` VERSION v15 → v16. | `public/index.html:26`, `public/leitung.html:23`, `:223` |
| qa-guardian | 1 | Prüfgerüst angelegt: `npm test`, 78 Prüfungen, ohne Netz und ohne Installation lauffähig (Bordmittel von Node, keine neue Abhängigkeit). Deckt Anmeldung/Sperre/Rechte, Ausgang und Offline, Vorgänge und Ereignisse, den Z-Bericht-Parser und die Projektregeln ab. Schema- und Fixture-Abgleich melden sich sichtbar als übersprungen, solange die Dateien fehlen. | `tests/` |
| hospitality-pro | 1→2 | Die Hilfe zum Restaurant-Schritt nannte feste Soll-Mengen im Fließtext. Das Soll steht ohnehin bei jedem Wein auf dem Schirm („Soll 3") — im Browser nachgesehen; sobald die Leitung eines ändert, widerspräche die Hilfe dem, was danebensteht. Zahlen raus, Verweis auf den Schirm rein. | `public/index.html:1422` (`HOWTO.tag[1]`) |
| hospitality-pro | 1→2 | Die Hilfe zum vierten Schritt hieß „Fassungsliste fotografieren", der Schritt heißt „Abschluss". Wer dort auf „?" tippt, bekam eine Foto-Anleitung. Heißt jetzt „Abschluss" und beantwortet, was „Fertig – Protokoll erstellen" auslöst (abschließen, senden, ohne Netz später) — das Foto bleibt ein Satz, solange der Schritt existiert. | `public/index.html:1429` (`HOWTO.tag[3]`) |
| hospitality-pro | 2 | Der Keller-Schritt meldete „Kein Wein zu holen. Alle Kühlschränke sind voll.", auch wenn Bar und Restaurant überhaupt nicht geprüft waren — ungeprüft gilt in der Fassung als voll. Wer Schritt 1 und 2 überspringt oder unterbricht, ging leer hinauf und merkte es am nächsten Abend am Tisch. Jetzt nennt die Meldung die Zahl der ungeprüften Plätze bzw. Laden und die Annahme dahinter; dieselbe Falle im Getränkelager mitbehoben. | `public/index.html:2650` (`rHol`), `:3635` (`gHolBlock`) |
| hospitality-pro | 2 | Der 409-Satz nannte den Vorgang nicht („Dein Stand von 20:40"). An einem Abend laufen leicht drei — Tagesfassung, Sonderentnahme, Nachfüllen; bei mehreren stand nur noch „3 Stände sind nicht übernommen". Jetzt steht der Name vorn wie eine Betreffzeile, mehrere werden aufgezählt, und ein Stand von gestern heißt „gestern 20:40" statt nur „20:40". | `public/index.html:1689` (`ueberholtSatz`) |

## Runde 14 (18.09.2026)
- **mittel** · Kellerzählung: auf Wunsch aus Runde 14 herausgehalten, eigene Runde nötig (Schrittweite, Reihen/Einzelflaschen, Trefferflächen).
- **niedrig** · Ringteilung liegt bei 45 px (32 Ring + 13 Luft). Der Griff ist auf die 44 px des Hauses gedeckelt; eine Teilung von genau 44 fällt beim Tasten an den Nachbarring.
- **niedrig** · Die Ziffern auf den Zählpunkten stehen absichtlich leise bei 1,9 : 1 — Hinweis der Messung, kein Urteil. Entscheidung, ob das so bleibt, steht aus.

## Runde 15 (18.09.2026) · aus den Berichten von Jäger und qa-guardian
- **hoch** · Der Grund der Sonderentnahme erreicht die Leitung nicht: keine Abfrage liest `ereignis.notiz` (`src/index.js:337`, `:415`, `:756`), `docNach()` nennt ihn nicht, das Backoffice auch nicht.
- **hoch** · Ein nachträglich geänderter Grund erzeugt keine Korrekturzeile — `ereignisseAbleiten()` vergleicht nur Mengen (`src/index.js:344–356`). Vorgang sagt „Küche", Journal sagt „Bruch".
- **hoch** · A4: Die Abgleich-Paarung im Backoffice ist seit F1 dauerhaft um einen Tag versetzt und steht nirgends im Backoffice.
- **mittel** · `summaryBig()` zeigt bei der Getränke-Sonderentnahme „0 Flaschen geholt" (liest `d.ent` statt `d.gent`). Steht wortgleich schon in `main`.
- **mittel** · Die Offline-Reihe bleibt bei dauerhaftem 4xx ≠ 409 stehen, `versuche` wird nie erhöht (`public/index.html:2101`). Fund aus Jagd 6.
- **mittel** · Zwei Listen für dieselben fünf Gründe: `GRUENDE` in der App, `GRUND_ERLAUBT` im Worker. Ein sechster Grund fiele still aus dem Journal.
- **mittel** · `src/stamm.json` und die Kopie in `public/index.html` sind auseinandergelaufen (`versetzt`, 18 `kurz`-Einträge, `pic`). `/api/stamm` hat keinen Verbraucher. `gastklein` = 8 steht in allen drei Kopien.
- **niedrig** · Veralteter Kommentar bei `.dot::after` — nennt 34 + 10 statt 32 + 13.
- **niedrig** · `ringMasse()` deckelt die Ringspalte nicht. Bei einem Soll über 4 stünden feste Spalten breiter als das Fenster. Heute unerreichbar (höchstes Soll einer Zählzeile ist 3).
- **niedrig** · `sw.js`: `activate` räumt den alten Vorrat auch dann, wenn `install` bei wackligem Netz nichts sammeln konnte.
- **niedrig** · CLAUDE.md widerspricht sich: harte Regel 1 nennt `v2-review`, der Abschnitt „Arbeitsweise" sagt eigener Branch. Ein `v2-review` gibt es weder lokal noch auf origin. Entscheidung von Casimir.

## Runde 15 (18.09.2026) · PIN
- **hoch** · Die Anmeldesperre zählt pro IP (`src/index.js`, `cf-connecting-ip`). Im Haus-WLAN teilen sich alle Geräte eine — zehn Vertipper an der Bar sperren den Keller mit aus, auch jemanden, der seinen Code weiß. Vorschlag: pro GERÄT zählen (`geraetId()` in index.html führt längst eine dauerhafte Kennung, sie müsste bei der Anmeldung mitgeschickt werden), die IP nur als lockerer Deckel gegen Fremde. Braucht eine additive Spalte auf `anmeldeversuch`, also eine Migration.
- **mittel** · Vier Stellen sind zehntausend Möglichkeiten, und jeder Code wird gegen jede Person gerechnet: bei zehn Personen trifft ein geratener Code rechnerisch einen von tausend Versuchen. Die gestaffelte Sperre ist das Einzige, was dagegensteht. Falls das Haus wächst, gehört die Anmeldung auf Name + PIN umgestellt — dann muss ein Code zu EINER Person passen.

## Runde 15 (18.09.2026) · zweite Schlusskontrolle qa-guardian
- **hoch** · **MERGE-SPERRE · `review/ERGEBNIS.md` weist beim Livegang falsch an.** Die Datei ist laut CLAUDE.md der PR-Text. Ihr Runbook stammt aus der Welt von 17.09. (sechs bis acht Ziffern) und wird von genau diesem Merge umgekehrt. Konkret: Auflage 4 (`:432`) „Erkennbar am Anmeldeschirm: **sechs** Kästchen … Stehen dort noch vier Kästchen, läuft die **alte** App" — nach dem Merge stehen dort VIER, er hielte den geglückten Deploy für gescheitert. Schritt 1 (`:460`) „neu laden, bis sechs Kästchen stehen" — das tritt nie ein. Schritt 3 und `:481` „sechs bis acht eigene Ziffern" / „Vierstellige Codes nimmt das Backoffice nicht mehr an" — heute umgekehrt, der Worker antwortet auf sechs Ziffern mit 422. Notweg Schritt 2 (`:489`) `POST /api/anlage` mit „Code sechs bis acht Ziffern" → 422, und das ist der Notweg. `:481` behauptet ausserdem „Aussperren kannst du dich dabei nicht" — falsch, siehe unten. Kopf (`:10`) nennt v36 und elf Commits; es sind v37 und 17. Die PIN-Umstellung, „PIN zurücksetzen" und die gestaffelte Sperre kommen gar nicht vor. | `review/ERGEBNIS.md:10`, `:248`, `:432`, `:451`, `:460`, `:481`, `:489`
- **hoch** · **MERGE-SPERRE · Vor dem Merge bestätigen: hat jede aktive Person genau vier Ziffern?** Der Worker prüft beim Anmelden keine Länge (`src/index.js:144`), die App lässt aber nichts Längeres mehr eintippen: `maxlength=4` (`public/index.html:2451`), Ziffernblock gedeckelt (`:2475`), Absenden bei der vierten (`:2473`), dazu Verwaltung (`:2742`) und Freigabe (`:5125`). `leitung.html` hat keine eigene Anmeldung und lebt vom Keks aus `index.html`. Wessen Code fünf bis acht Stellen hat, kommt nirgends mehr hinein. Es gibt keine Reihenfolge, die das umgeht: der heutige Live-Worker nimmt keinen vierstelligen Code entgegen, der neue keinen längeren. Rettung (nachgestellt, ohne D1-Konsole): `ANLAGE_OFFEN` im Dashboard setzen → `POST /api/anlage` mit vier Ziffern → 200 → anmelden → `ANLAGE_OFFEN` wieder löschen. Dauerhafte Lösung: die Anmeldung längere Codes annehmen lassen, Anzeige bei vier Feldern belassen. | `public/index.html:2451`, `:2473`, `:2475`, `:2742`, `:5125`
- **hoch** · **Der Persona-Durchlauf fällt seit Runde 15 sofort und liefert keinen Beleg mehr.** `tests/persona-tagesfassung.cjs:28` wurde auf vier Ziffern gestellt, die Bedienung nicht: nach der vierten Ziffer sendet die Seite selbst und leert das Feld, `[data-ok]` bleibt gesperrt, `click()` läuft 30 s in den Timeout und wirft. Rückgabe 1, aber hinter einer Pipe unsichtbar. Behebung: die beiden `locator("[data-ok]").click()` (`:175`, `:183`) entfallen. Kommentar `:27` nennt noch „Sechs Ziffern". Mit dieser einen Änderung läuft der ganze Weg bis zum Abschluss durch, ohne JS-Fehler — nachgestellt. | `tests/persona-tagesfassung.cjs:27`, `:175`, `:183`
- **mittel** · **Die Selbstschutz-Abfragen stehen nur im Browser, der Kommentar behauptet das Gegenteil.** `public/leitung.html:2570`: „Der Worker weist beides zusätzlich ab". Nachgestellt gegen den echten Worker: Selbstsperre → 200, `aktiv=0`, Anmeldung danach 401; Rollenwechsel auf sich selbst → 200, Rolle `service`, `/api/personen` danach 403. `personSchreiben(env, body)` bekommt `p` nicht übergeben und kann es nicht prüfen. Der Rollen-Auswahlkasten (`[data-rp]`) hat ausserdem gar keine Abfrage — ein Griff daneben, und die einzige Leitung ist draussen. Steht wortgleich schon in `main`. | `src/index.js:601`, `public/leitung.html:2570`, `:2583`
- **mittel** · **„PIN zurücksetzen" hat keinen Selbstschutz.** Der Nachbarknopf „Sperren" fragt bei der eigenen Person und bei der letzten Leitung nach, der neue nicht. Wer ihn auf der eigenen Zeile drückt, hat den alten Code sofort verloren (nachgestellt: 401) und den neuen genau einmal auf dem Schirm; beim einzigen Konto des Hauses hängt der Zugang an diesem Blatt. Vorschlag: bei der eigenen Zeile denselben Satz wie bei „Sperren" und den Hinweis in die Rückfrage aufnehmen. | `public/leitung.html:2639`, `:2648`
- **mittel** · **`tests/qa-schluss.cjs` schlägt falschen Alarm (2 von 24 nein).** Die beiden Prüfungen zum Getränke-Zweig erwarten noch, dass dort gar kein Grund verlangt wird; seit Runde 15 ist er gesetzt UND erfüllbar. Der Lauf müsste den Grund erst wählen. Eine Prüfreihe, die ohne Anlass rot ist, wird ab dem dritten Mal überlesen. | `tests/qa-schluss.cjs:127`, `:133`
- **mittel** · **`/api/person/pin` antwortet auf eine krumme `id` mit 500 statt 422.** `{"id":{…}}` oder `{"id":[…]}` gehen ungeprüft in `bind()`; der Fehler der Datenbank kommt als `fehler` zurück und landet mit Stapel im Log. Nur für die Leitung erreichbar, also kein Loch — aber 500 ist keine Antwort auf eine falsche Eingabe. Ein `typeof id !== "string"` genügt. Verwandt und älter: ein Keks, der kein Base64 ist, ergibt 500 statt 401 (`vonB64` in `tokenPruefen`). | `src/index.js:646`, `:121`
- **niedrig** · **Die Meldung am Anmeldeschirm nennt immer 15 Minuten.** Seit der Staffelung können es 30 oder 60 sein. Dazu: nach Ablauf der ersten Sperre bleiben die alten zehn Zeilen zwei Stunden im Gedächtnis, `uebrig` rechnet dann `max(0, 10−11)` = 0 und die App sagt „jetzt für 15 Minuten gesperrt", obwohl noch zehn Versuche frei sind (nachgestellt). | `public/index.html:2540`, `:2545`, `src/index.js:176`
- **niedrig** · **`anmeldeversuch` wächst unbegrenzt und hat keinen Index.** Geräumt werden nur die Fehlzeilen (`ok = 0`) und nur bei geglückter Anmeldung; die `ok = 1`-Zeilen bleiben für immer. Jede Anmeldung liest die Tabelle ohne Index auf `(ip, ts)` — heute ohne Wirkung, auf Jahre eine Bremse auf dem heissen Weg. Ein Index wäre additiv, also eine Migration. | `src/index.js:150`, `:184`

## Runde 16 (Nacht auf den 19.09.2026)

### Neu
- **mittel** · **Der Abgleich im Keller lässt den Offenausschank draußen.** `abgleichZeilen()` vergleicht Flaschen gegen `anzahl` des Z-Berichts und überspringt jede Position mit `ausschankMl` — aus Gläsern Flaschen zu rechnen braucht `mapping.gebinde_ml`, und die kennt `index.html` nicht (live hat ohnehin keine der 13 Zuordnungen eine Größe). Die Fußzeile sagt, wie viele es sind und wer sie rechnet. Sobald die Größen bestätigt sind, lohnt die Erweiterung: `gebinde_ml` über `/api/stamm` oder `/api/mapping` in die App, dann rechnet der Abgleich auch den Ausschank. | `public/index.html` (`abgleichZeilen`), `src/index.js` (`/api/mapping`)
- **niedrig** · **`POST /api/code` ist ein Orakel für vierstellige Codes — für Angemeldete.** Der Endpunkt verlangt eine Sitzung und zählt auf dieselbe IP-Sperre wie die Anmeldung ein (nachgestellt: zehn Fehlversuche dort sperren auch die Anmeldung). Wer schon drin ist, kann damit trotzdem Codes durchprobieren und stiehlt dabei dem ganzen Haus die Versuche. Sauberer wäre ein eigenes, engeres Maß je Sitzung. | `src/index.js` (`codeNachschlagen`)
- **niedrig** · **`.exprow` ist ungenutzt.** Die Klasse trug „Protokoll senden"; der Knopf ist in Runde 16 entfallen, die Regeln stehen noch. Mit ihr `.explesser button.del` (nur noch im Archiv gebraucht). | `public/index.html` (`.exprow`)
- **niedrig** · **Der Abgleich zeigt Artikel, die gefasst, aber nie verkauft wurden, als volle Abweichung.** Das ist richtig gerechnet (gefasst 2, verkauft 0 → +2), im Keller aber der häufigste Fall bei allem, was selten über den Tresen geht. Nach ein paar Tagen Betrieb prüfen, ob eine Schwelle („erst ab Differenz 2 zeigen") das Fenster nicht lesbarer macht. Braucht eine Antwort aus dem Service, nicht aus dem Code. | `public/index.html` (`abgleichZeilen`)

### Neu · Gegenprobe des qa-guardian (19.09.2026)
- **hoch** · **`verkaufteFlaschen()` rechnet ohne Menge im Kassennamen Stück = Flasche.** `flaschen()` in `public/leitung.html:1344` weist dieselbe Position als „Größe fehlt" aus und vergleicht sie NICHT. Am echten Bericht 37 belegt: „Aperol Spritz 1 Glas", 6 Stück, `ml = null`. Sobald jemand diese Kassenposition einem Artikel zuordnet — und genau dazu fordert der Morgenbrief auf — behauptet das Kellerfenster „verkauft 6 Flaschen". Gleicher Ausgang wie im Backoffice: ohne Menge im Namen wird nicht verglichen, die Position wird gezählt und im Fuß genannt. | `public/index.html` (`verkaufteFlaschen`)
- **hoch** · **Der Abschluss hat keine Zeitgrenze.** `holeZBericht()` und `holeGebindeGroessen()` rufen `fetch` ohne `AbortController`. An einem WLAN, das die Verbindung annimmt und nicht antwortet (Hotelnetz, Portal, schwacher Keller-AP), bleibt „Fertig – Speichern" auf „speichert …" und gesperrt stehen, bis der Browser von sich aus aufgibt. Verloren geht nichts — der Stand liegt im Gerät —, aber der Vorgang wird nicht abgeschlossen und geht nicht in den Ausgang. Das Muster steht seit dieser Runde im selben Haus: `serverAbmelden()`, `public/index.html:2202`, acht Sekunden. | `public/index.html` (`holeZBericht`, `holeGebindeGroessen`)
- **niedrig** · **„N Artikel verglichen" hat zweimal denselben Text** für Einzahl und Mehrzahl (`a.geprueft===1?" Artikel verglichen.":" Artikel verglichen."`). Sachlich richtig, aber die Verzweigung täuscht eine Unterscheidung vor, die es nicht gibt. | `public/index.html` (`popupAbgleich`)
- **niedrig** · **Artikelnamen brechen im Abgleich bei 320 px mitten im Wort** („Tom/ate", „Lein/dl · Lan/genl/ois"). Beleg: `review/screens/runde16/13-abgleich-fuss-320.png`. | `public/index.html` (`.abglz .nm`)
- **niedrig** · **Der erste „Neu"-Eintrag dieser Runde ist überholt:** Er beschreibt den Abgleich so, wie er vor der Berichtigung A2 gerechnet hat (Positionen mit `ausschankMl` werden übersprungen). Seit `verkaufteFlaschen()` gilt das nicht mehr. Beim nächsten Durchgang streichen. | `review/BACKLOG.md`

### Erledigt in Runde 16 (aus „Hoch"/„Runde 15" hierher verschoben)
- ~~**`abmelden` meldet nicht ab** (A1)~~ — der Knopf ruft `POST /api/abmelden` und wartet auf die Antwort; `leitung.html` hat einen eigenen Ausgang am Fuß der Navigation. Beleg: `review/screens/runde16/08…10`, Prüfung: `tests/runde16.test.mjs`, `tests/ui-runde16.cjs`.
- ~~**Zwei Personen dürfen denselben Code haben** (A2)~~ — `personSchreiben()` lehnt mit 409 ab, gezählt werden alle anderen Personen (auch gesperrte); `pinZuruecksetzen()` benutzt dieselbe Prüfung.
- ~~**Falsche Sperrmeldung / „immer 15 Minuten"** (B1)~~ — `versucheBisSperre()` und `sperrDauer()` im Worker, die App liest `uebrig`, `minuten` und `wartenBis` aus der Antwort.
- ~~**Stumme Schleife bei nicht schreibbarem `sessionStorage`** (B2)~~ — `setUser()` sagt, ob es angekommen ist; die Anmeldung meldet es.
- ~~**Zurückgesetzter Code gibt weiter frei** (B3)~~ — neuer Endpunkt `POST /api/code`; die App vergisst den toten Code dabei aus `hh_bekannt_v1`.
- ~~**Neuer Code geht bei Verbindungsabbruch verloren** (B4)~~ — das Backoffice würfelt ihn selbst und schickt ihn mit; `POST /api/person/pin` nimmt ihn optional entgegen und prüft Form und Dopplung.
- ~~**Veraltete Anweisung in den Unterlagen** (B5)~~ — `review/OFFENE-ENTSCHEIDUNGEN.md` Nr. 13 auf vier Stellen berichtigt und als ENTSCHIEDEN markiert, `review/MORGENBRIEF.md` neu geschrieben, `leitung.html` hat `PIN_LAENGE`.
- ~~**`/api/person/pin` antwortet auf eine krumme `id` mit 500**~~ — `typeof id !== "string"` → 422.
- ~~**`tests/qa-schluss.cjs` schlägt falschen Alarm**~~ — nachgeprüft: läuft grün, war schon in Runde 15 behoben.
- ~~**Der Persona-Durchlauf fällt**~~ — nachgeprüft: läuft durch; nur der Kommentar nannte noch sechs Ziffern, berichtigt.
- ~~**`review/ERGEBNIS.md` weist beim Livegang falsch an**~~ — die Datei beginnt jetzt mit Runde 16; der alte Runbook-Teil steht als „Vorgeschichte" darunter.
- ~~**`tests/ui-befunde.cjs` hatte zweimal den Schlüssel `r15`**~~ — drei Lagen wurden nie aufgenommen. Zu einer Liste zusammengezogen.

### Weiter offen, ausdrücklich nicht in dieser Runde angefasst
- **hoch** · **Gasteiner 0,25 l steht auf Soll 8.** Von Casimir physisch nachzuzählen. Die Zahl geht über `gFehlt()` ins append-only Journal; stimmt sie nicht, bucht jedes Nachfüllen dauerhaft falsch. In der Abschlussnachricht genannt. | `src/stamm.json`, `public/index.html`, `public/leitung.html`
- **hoch** · Anmeldesperre zählt pro IP (siehe Runde 15) — unverändert.
- **mittel** · Die Selbstschutz-Abfragen stehen nur im Browser. Der falsche Kommentar in `public/leitung.html` ist in Runde 16 berichtigt; der Riegel gehört weiter in den Worker und braucht `p` in `personSchreiben()`. | `src/index.js` (`personSchreiben`), `public/leitung.html`
- **mittel** · „PIN zurücksetzen" hat keinen Selbstschutz wie „Sperren" — unverändert. Die Rückfrage warnt inzwischen, wenn es die eigene Zeile trifft.
- **mittel** · Der Abgleich im Backoffice paart dauerhaft um einen Tag versetzt (Entscheidung Casimir nötig) — unverändert. Der neue Abgleich IM KELLER paart richtig (Z-Bericht des Vortags gegen die Fassung des Tages); die beiden widersprechen sich damit, bis das Backoffice nachzieht.

### Neu · zweite Jagd (19.09.2026, nach der Behebung)
- **mittel** · **Zwei Paarungen desselben Betriebstages, jetzt auf zwei Schirmen.** Der Abgleich im Keller paart Vorgang(T) mit Z-Bericht(T−1) — die Fassung am Morgen füllt nach, was am Abend davor verkauft wurde. Das Backoffice paart weiter Vorgang(T) mit Z-Bericht(T), im Mittagsblick durch `SPANNE=28` verschmiert, bei `abgleich(t)` ohne Spanne nicht. Beide können nicht recht haben. Braucht Casimirs Entscheidung (steht seit 18.09. offen, jetzt sichtbarer). | `public/index.html` (`vortagVon`), `public/leitung.html` (`abgleich`)
- **mittel** · **Zwei Rechnungen für dieselbe Umrechnung.** `verkaufteFlaschen()` in der App und `flaschen()` in `leitung.html` sind seit dieser Runde deckungsgleich im Ausgang (ohne Menge oder ohne Größe wird nicht verglichen), aber das Backoffice kennt zusätzlich die je ARTIKEL bestätigte Größe und zwei Vorschläge. Die App ist damit strenger, nie großzügiger. Jede Änderung an der einen gehört an der anderen nachgezogen — oder die Rechnung gehört an eine Stelle. | `public/index.html` (`verkaufteFlaschen`), `public/leitung.html:1344`
- **niedrig** · **`versuche:0`** wird in `inDenAusgang()` gesetzt und nirgends erhöht oder gelesen. Entweder zählen (und nach N Versuchen ins Sackfach) oder streichen. | `public/index.html` (`inDenAusgang`)
- **niedrig** · **`tests/ui-nachjagd.cjs` meldet einen Nicht-Lauf als Erfolg.** Bricht der Prüfserver mit `EADDRINUSE` ab (fester Port, zweiter Lauf parallel), endet die Datei mit Rückgabe 0. Eine Prüfreihe, die nicht gelaufen ist, darf nicht grün aussehen. Betrifft auch die anderen `.cjs`-Läufe mit festem Port. | `tests/ui-nachjagd.cjs`
- **niedrig** · Wortumbruch der Artikelnamen im Abgleichfenster bei 320 px — Beleg `review/screens/runde16/13-abgleich-fuss-320.png`.

### Erledigt in der zweiten Jagd
- ~~**`verkaufteFlaschen()` rechnet Stück = Flasche**~~ — der v24-Fehler des Backoffice, im Keller wieder aufgemacht. Jetzt wird ohne Menge im Namen oder ohne bestätigte Größe nicht verglichen.
- ~~**Der Abschluss hängt am schweigenden Server**~~ — `holeKurz()` mit acht Sekunden für alle vier wartenden Aufrufe. Belegt in `tests/ui-runde16.cjs`.
- ~~**`popupFertig()` verspricht einen Abgleich nach Kellerzählung, Wareneingang und Sonderentnahme**~~ — der Satz hängt jetzt am Modus.
- ~~**`/api/code` räumt die Sperre bei Erfolg nicht auf**~~ — tut es jetzt, wie `anmelden()`.

---

## Runde 16 · achte Jagd und zweite Gegenprobe (19.09.2026, nach dem Livegang)

| Prio | Punkt | Datei |
|---|---|---|
| mittel | **Feste Ports in den Prüfskripten.** `tests/ui-runde16.cjs` (8781), `tests/ui-leitung-echt.cjs` (8793), `qa-*` (8955 u. a.) binden feste Ports. Läuft zufällig ein zweiter Lauf, stirbt das Skript mit `EADDRINUSE` und Rückgabe 1, **ohne eine einzige Prüfung zu melden** — ein Ergebnis, das wie ein Fund aussieht und keiner ist; der stille Falschalarm in die andere Richtung ist ebenso möglich. Port `0` plus Weitergabe an die Seite wären drei Zeilen. (qa-guardian, zweite Gegenprobe) | `tests/*.cjs` |
| niedrig | **`hh_nkennung_v1` vergeht nicht mehr von selbst.** Seit dem Wechsel auf `localStorage` (achte Jagd · B, zweiter Tab) bleibt die Karte Name → Kennung stehen, bis `abmelden()` sie räumt oder der Browserspeicher geleert wird. Keine Codes darin (Regel 9 am laufenden Objekt geprüft), aber sie altert nicht. Ein Zeitstempel je Eintrag und ein Verfall nach 24 h wären sauberer. | `public/leitung.html` (`K_KENNUNG`) |
| niedrig | **Der Namenswächter im Worker ist ASCII-blind.** `lower()` in SQLite fasst „Müller" und „müller" als verschieden auf; die Wache greift dort also nicht. Die stärkere Hälfte ist die Kennung im Backoffice, die den Namen exakt trifft — aber zwei Geräte mit unterschiedlicher Groß-/Kleinschreibung eines Umlautnamens kämen durch. `NOCASE`-Collation oder Normalisierung im Worker. | `src/index.js` (`personSchreiben`) |
| niedrig | **Zwei Menschen mit exakt gleichem Namen brauchen jetzt einen Zusatz.** Das ist die Kehrseite des Wächters, entschieden in der Nacht ohne Rückfrage. Falls das im Haus stört: statt der Absage ein Rückfragedialog („‚Asad' gibt es schon — wirklich eine zweite Person?") mit einem ausdrücklichen Kennzeichen im Paket. | `src/index.js`, `public/leitung.html` (`#nAdd`) |

### Nachtrag · neunte Jagd und zweite Schlusskontrolle

| Prio | Punkt | Datei |
|---|---|---|
| niedrig | **Wettlauf beim Anlegen einer Person.** Zwei gleichzeitige `POST /api/personen` mit demselben neuen Namen laufen beide am `SELECT` des Namenswächters vorbei — beide 200, zwei Zeilen. Dicht macht das nur ein UNIQUE-Index auf `person(name)`, also eine additive Migration. **Vorher zwingend lesend prüfen, ob live schon gleichnamige Zeilen stehen**, sonst scheitert der Index beim Einspielen: `SELECT lower(trim(name)) n, COUNT(*) c FROM person GROUP BY n HAVING c > 1;` | `src/index.js` (`personSchreiben`), neue Migration |
| niedrig | **`hh_nkennung_v1` überlebt das Schließen des Tabs.** Nur `abmelden()` räumt. Wer den Tab zuklappt, lässt die Mitarbeiternamen im `localStorage` liegen — keine Codes, also keine Regel-9-Frage, aber am geteilten iPad ein Rest. Zusätzlich beim `pagehide` räumen, oder ein Zeitstempel je Eintrag mit Verfall nach 24 h. | `public/leitung.html` (`K_KENNUNG`) |
| niedrig | **Prüfungsreihenfolge in `personSchreiben`.** Die Namensprüfung läuft vor der PIN-Formprüfung: krummer Code + vorhandener Name gibt 409 statt 422. Über die Oberfläche unerreichbar (`#nAdd` prüft `PIN_MUSTER` selbst). | `src/index.js` |
| niedrig | **`name` als Objekt oder Array gibt 500 mit D1-Klartext.** Vorbestehend; die Meldung kommt jetzt aus dem neuen `SELECT` statt aus dem `INSERT`. Eine Typprüfung oben wäre ein Dreizeiler. | `src/index.js` (`personSchreiben`) |
| — | **Für Casimir, nicht für einen Agenten:** im Dashboard nachsehen, ob `ANLAGE_OFFEN` live noch gesetzt ist (`/api/ping` gibt `anlage: true/false` zurück). Solange sie gesetzt ist, steht `POST /api/anlage` unangemeldet offen — und der Körper bestimmt die Rolle selbst, **jeder Name legt also eine `leitung`-Zeile an**. Der Namenswächter dieser Nacht sperrt nur die exakte Wiederholung eines vorhandenen Namens; „Casimir." genügt, um an ihm vorbeizukommen. Das ist kein Schutz, sondern eine Bremse (zehnte Jagd · C). | Dashboard |

### Nachtrag · zehnte Jagd

| Prio | Punkt | Datei |
|---|---|---|
| mittel | **„Menge gesamt" auf der Seite „Zuordnung" summiert über ALLE geladenen Berichte (bis 60 Tage), die CSV über das Fenster (`SPANNE`).** Für dieselben 44 Kassennamen gemessen: 1293 gegen 885. Beide Zahlen sind für sich richtig, beide Skalen sind auf dem Schirm unbenannt. Entweder beide auf dieselbe Spanne, oder der Zeitraum gehört über die Tabelle. | `public/leitung.html` (`vZuordnung`) |
